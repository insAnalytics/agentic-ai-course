---
title: "Recap & Practice"
lesson: "10-the-tool-threat-model"
order: 5
---
import QuizGroup from "../../../../components/lesson/QuizGroup";
import MultiFileGradedExercise from "../../../../components/lesson/MultiFileGradedExercise";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

export const CATALOG_PY = String.raw`# a proposed toolset for an inbox-triage agent, up for security review -- read-only.
# each tool declares which trifecta legs it provides and whether it acts destructively.
TOOLSET = [
    {"name": "list_emails",   "reads_private_data": True,  "reads_untrusted_content": False, "sends_externally": False, "destructive": False},
    {"name": "read_email",    "reads_private_data": True,  "reads_untrusted_content": True,  "sends_externally": False, "destructive": False},
    {"name": "search_web",    "reads_private_data": False, "reads_untrusted_content": True,  "sends_externally": False, "destructive": False},
    {"name": "send_email",    "reads_private_data": False, "reads_untrusted_content": False, "sends_externally": True,  "destructive": False},
    {"name": "delete_email",  "reads_private_data": False, "reads_untrusted_content": False, "sends_externally": False, "destructive": True},
]
`;

export const AUDIT_STARTER = String.raw`LEG_KEYS = {
    "private_data": "reads_private_data",
    "untrusted_content": "reads_untrusted_content",
    "external_communication": "sends_externally",
}

def audit_toolset(toolset: list) -> dict:
    # TODO: list the tools that provide each leg, flag the trifecta, and list destructive tools
    ...

def cut_external_leg(toolset: list) -> list:
    # TODO: return the toolset without the tools that send externally
    ...
`;

export const AUDIT_REFERENCE = String.raw`LEG_KEYS = {
    "private_data": "reads_private_data",
    "untrusted_content": "reads_untrusted_content",
    "external_communication": "sends_externally",
}

def audit_toolset(toolset: list) -> dict:
    legs = {leg: [t["name"] for t in toolset if t[key]] for leg, key in LEG_KEYS.items()}
    vulnerable = all(legs.values())
    destructive = [t["name"] for t in toolset if t["destructive"]]
    return {"legs": legs, "vulnerable_to_trifecta": vulnerable, "destructive_tools": destructive}

def cut_external_leg(toolset: list) -> list:
    # the design fix from this lesson: remove the tools that form the external-communication leg
    return [t for t in toolset if not t["sends_externally"]]
`;

export const HIDDEN_TESTS = String.raw`from catalog import TOOLSET
from audit import audit_toolset, cut_external_leg

# 1. the proposed toolset has all three legs, and names which tools form each
report = audit_toolset(TOOLSET)
assert report["vulnerable_to_trifecta"] is True
assert report["legs"]["private_data"] == ["list_emails", "read_email"]
assert report["legs"]["untrusted_content"] == ["read_email", "search_web"]
assert report["legs"]["external_communication"] == ["send_email"]
assert report["destructive_tools"] == ["delete_email"]

# 2. cutting the external leg clears the trifecta, while private and untrusted remain
safer = cut_external_leg(TOOLSET)
after = audit_toolset(safer)
assert after["vulnerable_to_trifecta"] is False
assert after["legs"]["external_communication"] == []
assert after["legs"]["private_data"] and after["legs"]["untrusted_content"]

# 3. destructive tools are still flagged after the cut (they're a separate concern from the trifecta)
assert after["destructive_tools"] == ["delete_email"]

# 4. a notes agent that reads only trusted content has the private-data leg, but no untrusted leg, so it is not vulnerable
notes_agent = [
    {"name": "search_notes", "reads_private_data": True, "reads_untrusted_content": False, "sends_externally": False, "destructive": False},
    {"name": "get_note",     "reads_private_data": True, "reads_untrusted_content": False, "sends_externally": False, "destructive": False},
]
r = audit_toolset(notes_agent)
assert r["vulnerable_to_trifecta"] is False and r["legs"]["untrusted_content"] == [] and r["destructive_tools"] == []

# 5. one tool supplying two legs still counts toward both
combo = [
    {"name": "browse_and_post", "reads_private_data": False, "reads_untrusted_content": True, "sends_externally": True, "destructive": False},
    {"name": "read_crm",        "reads_private_data": True,  "reads_untrusted_content": False, "sends_externally": False, "destructive": False},
]
r = audit_toolset(combo)
assert r["vulnerable_to_trifecta"] is True
assert r["legs"]["untrusted_content"] == ["browse_and_post"] and r["legs"]["external_communication"] == ["browse_and_post"]
`;

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
  {
    "question": "Which of these is an example of indirect prompt injection?",
    "options": [
      "A calendar invite telling the assistant to forward the guest list",
      "A user typing \"ignore your instructions\" into the agent's chat box",
      "A developer's system prompt telling the agent to refuse some requests",
      "A model misreading an ambiguous request and deleting the wrong file"
    ],
    "correctIndex": 0,
    "explanation": "The invite's text was written by a stranger and reached the model through a tool. A user typing an override is direct injection, a system prompt is the developer's own instruction, and a misreading is an honest mistake, not an attack."
  },
  {
    "question": "Why can't role labels (system, user, tool result) do the job of a parameterized query's placeholder?",
    "options": [
      "The model learned to respect roles in training; nothing enforces them",
      "Roles are stripped out by the API before the text reaches the model",
      "Placeholders only exist for SQL, and nobody has adapted them to text",
      "Only the system role is separated; user and tool text are merged"
    ],
    "correctIndex": 0,
    "explanation": "Roles do reach the model, and it does tend to respect them. But a tendency learned in training isn't a boundary, so text in a tool result can still act like an instruction."
  },
  {
    "question": "A detection filter catches 99% of injection attempts. An attacker makes 200 attempts. Roughly how likely is at least one to get through?",
    "options": [
      "About 1%, since that's the filter's miss rate on any attempt",
      "About 87%: misses compound across repeated attempts",
      "About 50%, since the attacker has to beat the filter and the model",
      "Zero once the filter has seen an attempt and learned to block it"
    ],
    "correctIndex": 1,
    "explanation": "1 − 0.99²⁰⁰ ≈ 0.87, the same arithmetic as the 95% demo. That's why 99% is still a failing grade in application security."
  },
  {
    "question": "Which change removes the lethal trifecta from the inbox-triage agent?",
    "options": [
      "Adding \"never forward emails to outside addresses\" to its system prompt",
      "Wrapping each email body in <untrusted> tags before the model sees it",
      "Letting `send_email` deliver only to the user's own address",
      "Switching to a newer model that's trained to resist injection"
    ],
    "correctIndex": 2,
    "explanation": "Only that one removes a leg: with no way to send to an outsider, there's no external channel. The other three are mitigations that make a leak less likely, not impossible."
  },
  {
    "question": "One tool, `summarise_url(url)`, fetches any page and returns a summary. Which legs can it supply on its own?",
    "options": [
      "Untrusted content, and external communication via the URL",
      "Only untrusted content, since all it does is read a page",
      "Only external communication, since summaries are generated, not read",
      "None, as long as it returns a summary rather than the raw page"
    ],
    "correctIndex": 0,
    "explanation": "The page is untrusted content, and a summary can still carry an injected instruction. Because it fetches any URL, an injection can also put a secret in the URL, so the same tool is an external channel."
  },
  {
    "question": "Two agents both hold `delete_customer`. One reads only internal dashboards; the other reads public support tickets. Which statement is right?",
    "options": [
      "Same blast radius, but the ticket agent is exposed to strangers",
      "They're equally at risk, since both hold exactly the same tool",
      "The dashboard agent is riskier, since internal data is more sensitive",
      "Neither is exposed, since deletion isn't part of the lethal trifecta"
    ],
    "correctIndex": 0,
    "explanation": "Blast radius is set by the tools, so the worst case is the same. Exposure differs: strangers can put text in front of the ticket agent, so that's the one whose delete most needs narrowing or a gate."
  },
  {
    "question": "Why does the audit flag `delete_email` separately, when it provides no trifecta leg?",
    "options": [
      "A fooled agent can still destroy data with it, even if nothing leaks",
      "Destructive tools always count as the external-communication leg",
      "The audit flags every tool that isn't read-only, as a formality",
      "Deleting an email sends a notification to its original sender"
    ],
    "correctIndex": 0,
    "explanation": "The trifecta is about data leaving. Destruction is a different harm inside the blast radius, so it needs its own flag."
  },
  {
    "question": "Why is blast radius something you can design around, when injection itself can't be prevented?",
    "options": [
      "It's set by tools and permissions, which are ordinary code you control",
      "It shrinks automatically as models get better at resisting injection",
      "It only depends on how much untrusted content the agent happens to read",
      "It's measured after an attack, so you can patch the specific hole"
    ],
    "correctIndex": 0,
    "explanation": "You can't decide what the model will be talked into, but you do decide what tools it has and how far they reach. That's the whole premise of the next lesson."
  }
]}
/>

</CheckpointZone>

<CheckpointZone>

<MultiFileGradedExercise
  client:load
  task={"A proposed inbox-triage toolset is up for review in catalog.py, which is read-only. Each tool declares which trifecta legs it provides (reads_private_data, reads_untrusted_content, sends_externally) and whether it's destructive. Complete audit.py. audit_toolset(toolset) returns: legs, a dict with keys private_data, untrusted_content and external_communication, each mapping to the list of tool names that provide that leg, in toolset order; vulnerable_to_trifecta, True only when all three legs have at least one tool; and destructive_tools, the names of the destructive tools. cut_external_leg(toolset) returns a new toolset with the tools that send externally removed, the structural fix from this lesson."}
  entry="audit.py"
  files={[
    { name: "catalog.py", readOnly: true, code: CATALOG_PY },
    { name: "audit.py", code: AUDIT_STARTER },
  ]}
  hiddenTests={HIDDEN_TESTS}
  hint={"For legs, a dict comprehension over LEG_KEYS pairs each leg name with [t[\"name\"] for t in toolset if t[key]]. vulnerable_to_trifecta is all(legs.values()), since an empty list is falsy. cut_external_leg is a list comprehension keeping tools where not t[\"sends_externally\"]."}
  correctFiles={[
    { name: "catalog.py", readOnly: true, code: CATALOG_PY },
    { name: "audit.py", code: AUDIT_REFERENCE },
  ]}
  explanation={"This is the whole lesson as a tool a team would actually run before shipping. The audit names which tools form each leg (test 1), not just whether the trifecta is present, so a reviewer knows what to change. Test 2 applies the structural fix: cut the external leg and the trifecta clears, while the agent keeps reading private and untrusted content, exactly the kind of tradeoff the next lesson makes deliberately. Test 3 is a reminder that destructive tools are a separate worry from the trifecta: delete_email sends nothing outward, but a compromised agent using it still does real damage, which is why the audit flags it on its own. Tests 4 and 5 cover the two ends: an agent that reads private data but no untrusted content has only one leg and isn't exposed to the trifecta, and one tool can supply two legs at once."}
/>

</CheckpointZone>
