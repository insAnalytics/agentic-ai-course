---
title: "Names and descriptions as prompts"
lesson: "01-writing-tools-models-can-use"
order: 1
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

A tool's name and description aren't documentation for a future developer
reading the code —
[they're the exact text the model sees when deciding whether and how to call a tool](/01-llm-foundations/10-structured-output-and-tool-calling/04-tool-calling/#the-model-doesn-t-call-anything-it-produces-structured-data).
Writing a vague name or description isn't a stylistic shortcut; it's the
same category of mistake as
[a vague prompt from Module 2](/02-the-agent-loop/02-prompting-fundamentals/01-specificity-and-clear-instructions/#the-pain-a-vague-instruction-leaves-too-much-to-guess),
just now shaping a tool-selection decision instead of a written response.

<Subsection title="The pain: a name and description that don't actually distinguish intent" variant="a">

```python
def get_data(id: str) -> dict:
    """Gets data."""
    ...
```

Nothing here tells the model *what kind* of data, *what* `id` actually
identifies, or when this tool is even the right one among several
similarly-vague options. A model choosing between this and a
similarly-underspecified second tool has no real basis to pick
correctly — not because it reasoned poorly, but because the tool never
gave it enough to reason *with*.

</Subsection>

<Subsection title="The fix: specific enough to actually disambiguate" variant="b">

```python
def get_user_profile(user_id: str) -> dict:
    """
    Retrieve a user's profile (name, email, account status) by their
    unique user ID. Use this when you need account details for a
    specific, already-identified user — not for searching by name.
    """
    ...
```

The description now does real work: it states exactly what's returned,
what `user_id` means, and — importantly — an explicit boundary
("not for searching by name") ruling out a plausible misuse before it
happens. This is
[the same always/never constraint pattern from Module 2's system-prompt design](/02-the-agent-loop/03-the-system-prompt-as-agent-design/02-role-persona-and-behavioral-constraints/#role-and-explicit-behavioral-constraints),
applied at the individual-tool level rather than the whole agent's
behavior.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Who is a tool's name and description actually written for?",
      options: [
        "A future human developer reading the source code",
        "The model itself — this is text it directly reasons over when deciding whether and how to call the tool",
        "No one — names and descriptions have no functional effect",
        "Only the person who originally wrote the tool",
      ],
      correctIndex: 1,
      explanation:
        "The name and description are exactly what the model sees when choosing among tools and deciding how to call one — not comments for a reader of the code.",
    },
    {
      question: "Why does get_data(id)'s vague name and description create a real problem, not just a style issue?",
      options: [
        "It doesn't — vague names have no actual consequence",
        "It gives the model no real basis to correctly choose this tool over a similarly-vague alternative, or to know what id should actually be",
        "Python requires descriptive names for functions to run at all",
        "Vague names only matter for tools with more than one parameter",
      ],
      correctIndex: 1,
      explanation:
        "A model choosing between two underspecified tools has nothing to reason with — the vagueness is a real decision-making problem, not merely unclear style.",
    },
    {
      question: 'What does "not for searching by name" accomplish in the get_user_profile description?',
      options: [
        "Nothing — it's redundant with the rest of the description",
        "It's an explicit constraint ruling out a plausible misuse before it happens, the same always/never pattern from Module 2's system-prompt design",
        "It prevents the tool from ever being called incorrectly, with certainty",
        "It only applies if the tool has more than one parameter",
      ],
      correctIndex: 1,
      explanation:
        "Like a system prompt's always/never constraints, this rules out a specific plausible mistake in advance — a strong nudge, not a mechanical guarantee.",
    },
  ]}
/>

</CheckpointZone>
