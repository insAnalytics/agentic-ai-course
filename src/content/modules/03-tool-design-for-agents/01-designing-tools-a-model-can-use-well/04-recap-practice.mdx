---
title: "Recap & Practice"
lesson: "01-designing-tools-a-model-can-use-well"
order: 4
---
import QuizGroup from "../../../../components/lesson/QuizGroup";
import GradedExercise from "../../../../components/lesson/GradedExercise";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Who is a tool's name and description actually written for?",
      options: [
        "A future human developer",
        "The model itself — text it directly reasons over",
        "No one — no functional effect",
        "Only the tool's original author",
      ],
      correctIndex: 1,
      explanation:
        "A tool's name and description are exactly what the model sees when choosing among tools and deciding how to call one.",
    },
    {
      question: "Why does get_data(id)'s vague description create a real problem?",
      options: [
        "It doesn't",
        "No real basis for the model to choose it correctly over an alternative",
        "Python requires descriptive names to run",
        "Only matters with multiple parameters",
      ],
      correctIndex: 1,
      explanation:
        "A model choosing between two underspecified tools has nothing to reason with — that's a real decision-making problem, not a style issue.",
    },
    {
      question: "Why doesn't a valid schema guarantee well-designed parameters?",
      options: [
        "It does guarantee this",
        "The schema only constrains shape; choosing clear names and the right types is a separate decision",
        "Schemas can't be generated for tool parameters",
        "This contradicts Module 1's coverage",
      ],
      correctIndex: 1,
      explanation:
        "Constrained decoding enforces the shape a schema declares, but nothing about the schema itself decides whether that shape is a good design.",
    },
    {
      question: 'What makes a tool "narrow"?',
      options: [
        "A shorter name",
        "It does exactly one thing, leaving little room to call it wrong",
        "It never returns data",
        "More parameters than a broad tool",
      ],
      correctIndex: 1,
      explanation:
        "A narrow tool's single, well-defined purpose leaves little room for a model to get the call wrong.",
    },
    {
      question: "What does broad-tool flexibility trade away?",
      options: [
        "Nothing — strictly better",
        "More room for the model to get a more complex call wrong",
        "Execution speed",
        "The possibility of misuse",
      ],
      correctIndex: 1,
      explanation:
        "A broad tool covers more ground, but asks the model to correctly construct something more complex on every single call.",
    },
    {
      question: "What does the model actually receive for each tool?",
      options: [
        "The function's source code",
        "Its name, its description and a JSON schema for its arguments",
        "Only its name",
        "A test suite for the tool",
      ],
      correctIndex: 1,
      explanation:
        "Name, description and argument schema are the whole of what the model sees. The function body never reaches it.",
    },
    {
      question: "Which parameter design puts the most useful information where the model can see it?",
      options: [
        "status: str with a # comment listing the allowed values",
        "status: Literal[\"active\", \"paused\", \"retired\"] = Field(description=\"...\")",
        "s: str",
        "filters: str holding a JSON object",
      ],
      correctIndex: 1,
      explanation:
        "The Literal becomes an enum in the schema and the Field description is written into it. A comment never reaches the model, and a JSON string hides its structure from the schema.",
    },
    {
      question: "When does a composite tool like clone_agent make sense?",
      options: [
        "Always — fewer tools is always better",
        "For a common sequence that's always done the same way, where one call saves loop steps and resent context",
        "Never — every tool should do exactly one thing",
        "Only when the model can't call two tools",
      ],
      correctIndex: 1,
      explanation:
        "Combining a common multi-step sequence into one call means fewer model calls and less resent history, without making the model's job harder.",
    },
  ]}
/>

</CheckpointZone>

<CheckpointZone>

<GradedExercise
  client:load
  task={"Given this tool from [the agent registry app introduced back in Module 0](/00-python-essentials/09-building-apis-with-fastapi/11-recap-practice/) — redesign it as a well-designed, narrow tool for looking up a single registered agent's configuration by name. Your version must have: a descriptive function name (not q), a docstring specifying exactly what's returned and what the parameter means, a single, clearly named and type-hinted parameter, and (in a comment) a one-line justification for why this stays narrow rather than becoming a broader run_query-style tool."}
  starterCode={`def q(s: str) -> str:
    """Query."""
    ...`}
  hiddenTests={[
    `import types
_fns = [v for k, v in globals().items() if isinstance(v, types.FunctionType) and not k.startswith("__")]
assert len(_fns) == 1
assert _fns[0].__name__ != "q"`,
    `import types, inspect
_fn = [v for k, v in globals().items() if isinstance(v, types.FunctionType) and not k.startswith("__")][0]
_doc = _fn.__doc__ or ""
_params = list(inspect.signature(_fn).parameters)
_param_name = _params[0].lower() if _params else ""
_param_as_words = _param_name.replace("_", " ")
assert len(_doc.strip()) > len("Query.")
assert _params and (_param_name in _doc.lower() or _param_as_words in _doc.lower())
assert "agent" in _doc.lower()`,
    `import types, inspect
_fn = [v for k, v in globals().items() if isinstance(v, types.FunctionType) and not k.startswith("__")][0]
_sig = inspect.signature(_fn)
_params = list(_sig.parameters.values())
assert len(_params) == 1
assert _params[0].annotation is str`,
    `_src_lines = __source__.splitlines()
_comment_lines = [l for l in _src_lines if "#" in l]
_comment_text = " ".join(_comment_lines).lower()
assert any(word in _comment_text for word in ("narrow", "broad", "granular"))`,
  ]}
  hint={"Compose the whole lesson: a name and docstring specific enough to disambiguate, exactly as in [the names-and-descriptions concept](/03-tool-design-for-agents/01-designing-tools-a-model-can-use-well/01-names-and-descriptions-as-prompts/); a parameter whose name says what it is (agent_name, not s), as in [the parameter-design concept](/03-tool-design-for-agents/01-designing-tools-a-model-can-use-well/02-parameter-design/); and a one-line comment stating this stays narrow on purpose, per [the granularity concept](/03-tool-design-for-agents/01-designing-tools-a-model-can-use-well/03-granularity-narrow-vs-broad-tools/)."}
  correctAnswer={{
    code: `def get_agent_config(agent_name: str) -> dict:
    """
    Retrieve one agent's configuration (model, creation date) by its
    exact registered name. Use this for a specific, already-known
    agent name -- not for searching or listing multiple agents.
    """
    # kept narrow: a single, unambiguous lookup is far less error-prone
    # for the model to call correctly than a general query tool would be
    ...`,
    explanation:
      "Every piece here composes something from this lesson: get_agent_config and its docstring are specific enough that the model knows exactly what it returns and what agent_name means; the parameter is still a str, since a name is a string, but agent_name plus the docstring's \"exact registered name\" now say what that string must be, where s said nothing; and the trailing comment states the granularity tradeoff explicitly, the same reasoning as run_sql's broader, riskier alternative.",
  }}
/>

</CheckpointZone>

<CheckpointZone>

<GradedExercise
  client:load
  task={'Comprehensive sandbox: redesign a small registry toolset. The starter has three vague tools, each with an args model: data (look up one agent), find (search by model and status) and query (a broad tool that runs any filter expression). Replace them with exactly these: get_agent_config(agent_name) with GetAgentConfigArgs; search_agents(model, status) with SearchAgentsArgs, where status only allows "active", "paused" or "retired"; and list_agents(include_archived=False, limit=20) with ListAgentsArgs, where include_archived is a boolean and limit an integer. Remove data, find and query completely (and their args models): search_agents and list_agents cover what query was used for, without a free-form expression. Every docstring needs "Returns", "Use when" and "Not for"; every field needs a Field(description=...); and each function\'s parameters must match its args model\'s fields, in the same order. Bodies can stay as `...`.'}
  starterCode={`from typing import Literal
from pydantic import BaseModel, Field

class DataArgs(BaseModel):
    id: str

def data(id: str) -> dict:
    """Gets data."""
    ...

class FindArgs(BaseModel):
    m: str
    s: str

def find(m: str, s: str) -> list:
    """Finds agents."""
    ...

class QueryArgs(BaseModel):
    q: str

def query(q: str) -> list:
    """Runs a filter expression over the registry, e.g. "model == 'claude-haiku' and status == 'paused'"."""
    ...`}
  hiddenTests={[
    `for gone in ("data", "find", "query", "DataArgs", "FindArgs", "QueryArgs"):
    assert gone not in globals(), gone
for name in ("get_agent_config", "search_agents", "list_agents", "GetAgentConfigArgs", "SearchAgentsArgs", "ListAgentsArgs"):
    assert name in globals(), name`,
    `for tool in (get_agent_config, search_agents, list_agents):
    doc = (tool.__doc__ or "").lower()
    assert "return" in doc and "use when" in doc and "not for" in doc, tool.__name__`,
    `import inspect
for tool, args in ((get_agent_config, GetAgentConfigArgs), (search_agents, SearchAgentsArgs), (list_agents, ListAgentsArgs)):
    props = args.model_json_schema()["properties"]
    assert list(inspect.signature(tool).parameters) == list(props), tool.__name__
    assert all(p.get("description", "").strip() for p in props.values()), args.__name__`,
    `schema = SearchAgentsArgs.model_json_schema()
assert set(schema["properties"]["status"].get("enum", [])) == {"active", "paused", "retired"}
assert sorted(schema.get("required", [])) == ["model", "status"]
assert GetAgentConfigArgs.model_json_schema().get("required") == ["agent_name"]`,
    `schema = ListAgentsArgs.model_json_schema()
props = schema["properties"]
assert props["include_archived"].get("type") == "boolean" and props["include_archived"].get("default") is False
assert props["limit"].get("type") == "integer" and props["limit"].get("default") == 20
assert not schema.get("required")`,
  ]}
  hint={"Work through the lesson in order for each tool. Name and docstring: say what it returns, when to use it, and when not to (naming the tool to use instead). Parameters: clear names, Literal for status, bool and int with defaults for list_agents, and a Field(description=...) on every field. Granularity: query goes away entirely, because search_agents and list_agents cover its real uses with bounded, typed parameters. Finally, make sure each function's parameter list matches its args model field for field."}
  correctAnswer={{
    code: `from typing import Literal
from pydantic import BaseModel, Field

class GetAgentConfigArgs(BaseModel):
    agent_name: str = Field(description="Exact registered name of the agent, e.g. 'research_agent'.")

def get_agent_config(agent_name: str) -> dict:
    """Look up one registered agent by its exact name.

    Returns that agent's configuration as a dict (model, status, created date).
    Use when you already know the agent's exact name.
    Not for finding agents by model or status -- use search_agents for that.
    """
    ...

class SearchAgentsArgs(BaseModel):
    model: str = Field(description="Only agents running on this model, e.g. 'claude-haiku'.")
    status: Literal["active", "paused", "retired"] = Field(description="Only agents with this status.")

def search_agents(model: str, status: str) -> list:
    """Find registered agents by model and status.

    Returns a list of matching agent names (possibly empty).
    Use when you need every agent matching both a model and a status.
    Not for one agent's full configuration -- use get_agent_config for that.
    """
    ...

class ListAgentsArgs(BaseModel):
    include_archived: bool = Field(default=False, description="Also include archived agents.")
    limit: int = Field(default=20, description="Maximum number of agent names to return.")

def list_agents(include_archived: bool = False, limit: int = 20) -> list:
    """List registered agent names, newest first.

    Returns up to limit agent names.
    Use when you need an overview of which agents exist.
    Not for filtering by model or status -- use search_agents for that.
    """
    ...`,
    explanation:
      "Each part of the lesson is checked through the text the model would actually receive. The docstrings (__doc__) say what each tool returns and when to use it, and they point at each other. The schemas (model_json_schema()) have clear names, a status enum, a real boolean, defaults for the optional fields and a description on every field. The broad query tool is gone, replaced by two bounded tools that cover what it was used for.",
  }}
/>

</CheckpointZone>
