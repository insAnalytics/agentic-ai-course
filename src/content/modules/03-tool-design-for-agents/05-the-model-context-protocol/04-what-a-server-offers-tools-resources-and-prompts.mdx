---
title: "What a server offers: tools, resources and prompts"
lesson: "05-the-model-context-protocol"
order: 4
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import LiveDemo from "../../../../components/lesson/LiveDemo";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import GradedExercise from "../../../../components/lesson/GradedExercise";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

export const HOST_DEMO = String.raw`# how a host uses each primitive -- the difference is who decides

# 1. tools: offered to the model, which decides whether to call them
tool = {
    "name": "get_agent_model",
    "description": "Return the model one registered agent runs on, by its exact name.",
    "inputSchema": {"type": "object", "properties": {"agent_name": {"type": "string"}}, "required": ["agent_name"]},
}
offered_to_model = [{"name": tool["name"], "description": tool["description"], "input_schema": tool["inputSchema"]}]
print("tool offered to the model:", offered_to_model[0]["name"])

# 2. resources: read by the host, which decides to put them in front of the model
read_result = {
    "resultType": "complete",
    "contents": [{"uri": "registry://policies/naming", "mimeType": "text/markdown",
                  "text": "Agent names are lowercase, end in _agent, and use underscores."}],
}
policy_text = read_result["contents"][0]["text"]
user_message = f"Suggest a name for an agent that triages bug reports.\n\n<policy>\n{policy_text}\n</policy>"
print("resource attached by the host:\n" + user_message)

# 3. prompts: chosen by the user, and their messages start the conversation
prompt_result = {
    "resultType": "complete",
    "description": "Review an agent's configuration",
    "messages": [{"role": "user", "content": {"type": "text",
                  "text": "Review research_agent's configuration. Check its model is current and its name follows policy."}}],
}
messages = [{"role": m["role"], "content": m["content"]["text"]} for m in prompt_result["messages"]]
print("conversation started from the user's chosen prompt:", messages)`;

export const STARTER = String.raw`def resource_to_context(read_result: dict) -> str:
    # TODO: wrap each content item in a <resource> tag, following the rules above
    ...`;

export const REFERENCE = String.raw`def resource_to_context(read_result: dict) -> str:
    blocks = []
    for item in read_result.get("contents", []):
        if "text" in item:
            blocks.append(f'<resource uri="{item["uri"]}">\n{item["text"]}\n</resource>')
        else:
            mime_type = item.get("mimeType", "unknown type")
            blocks.append(f'<resource uri="{item["uri"]}">[binary content ({mime_type}) not shown]</resource>')
    return "\n".join(blocks)`;

export const TESTS = [
  String.raw`# one text item, wrapped exactly
one = {"resultType": "complete", "contents": [
    {"uri": "registry://policies/naming", "mimeType": "text/markdown", "text": "Names end in _agent."}]}
assert resource_to_context(one) == '<resource uri="registry://policies/naming">\nNames end in _agent.\n</resource>'`,
  String.raw`# two items, both included, in order
two = {"resultType": "complete", "contents": [
    {"uri": "registry://agents/research_agent", "text": "model: claude-sonnet"},
    {"uri": "registry://agents/support_agent", "text": "model: claude-haiku"}]}
out = resource_to_context(two)
assert out.count("<resource ") == 2 and out.index("research_agent") < out.index("support_agent")
assert "model: claude-haiku" in out`,
  String.raw`# binary content is replaced by a placeholder naming its mimeType
binary = {"resultType": "complete", "contents": [
    {"uri": "registry://diagrams/topology.png", "mimeType": "image/png", "blob": "iVBORw0KGgoAAAANSUhEUg=="}]}
out = resource_to_context(binary)
assert "iVBORw0" not in out and "image/png" in out and 'uri="registry://diagrams/topology.png"' in out`,
  String.raw`# empty contents gives an empty string
assert resource_to_context({"resultType": "complete", "contents": []}) == ""`,
];

<Subsection title="Three kinds of thing, sorted by who decides" variant="a">

So far this lesson has talked about MCP servers as if they only offer
tools. Tools are the most used part, but a server can offer three kinds
of thing, which the spec calls **primitives**:

| Primitive | Who decides to use it | What it is | Example |
|---|---|---|---|
| **Tools** | the model | functions the model can call to act or look something up | "get this agent's model", "create an agent" |
| **Resources** | the application (host) | data the host can read and put in front of the model | a policy document, a config file, a database record |
| **Prompts** | the user | ready-made message templates a person picks to start a task | a "review this agent's config" menu item or slash command |

The column that matters is the second one. All three end up as text the
model reads, but they differ in *who pulls the trigger*:

- **A tool** is chosen by the model, mid-loop, exactly like every tool
  since
  [Module 2](/02-the-agent-loop/04-writing-the-loop-by-hand/02-from-round-trip-to-loop/#the-actual-loop-written-and-running-for-real).
  The host offers the model the tool's name, description and schema,
  and the model decides whether and when to call it.
- **A resource** is chosen by the host application. The model never
  asks for it directly. The host reads it, perhaps because the user
  attached it in the interface, or because the application always
  includes it, and then places its contents into the conversation.
- **A prompt** is chosen by a person. The application shows the
  server's prompts as menu options or commands, and when someone picks
  one, its messages start the conversation.

</Subsection>

<Subsection title="What a server lists" variant="b">

Each primitive has a `list` method that returns what the server offers,
plus a method to use one. A server says which primitives it supports in
the `capabilities` it returns from
[`server/discover`](/03-tool-design-for-agents/05-the-model-context-protocol/03-stateless-by-design/#server-discover-asking-before-you-start),
and a client only asks for the ones that are there. Here's what a small
registry server might return from each list:

<LiveDemo client:load code={`# what a small registry server might return from each of its three list methods
tools_list = {
    "resultType": "complete",
    "tools": [{
        "name": "get_agent_model",
        "title": "Look up an agent's model",
        "description": "Return the model one registered agent runs on, by its exact name.",
        "inputSchema": {
            "type": "object",
            "properties": {"agent_name": {"type": "string", "description": "Exact registered name of the agent."}},
            "required": ["agent_name"],
        },
    }],
}

resources_list = {
    "resultType": "complete",
    "resources": [{
        "uri": "registry://policies/naming",
        "name": "naming-policy",
        "title": "Agent naming policy",
        "mimeType": "text/markdown",
    }],
}

prompts_list = {
    "resultType": "complete",
    "prompts": [{
        "name": "review_agent_config",
        "title": "Review an agent's configuration",
        "arguments": [{"name": "agent_name", "description": "The agent to review", "required": True}],
    }],
}

print("tools     ->", [t["name"] for t in tools_list["tools"]])
print("resources ->", [r["uri"] for r in resources_list["resources"]])
print("prompts   ->", [p["name"] for p in prompts_list["prompts"]])`} />

*These are example results in the shapes the spec defines.*

The methods, as a quick reference:

- **Tools:** `tools/list` returns each tool's `name`, `description` and
  `inputSchema`; `tools/call` runs one, with a `name` and `arguments`.
- **Resources:** `resources/list` returns what's available, each
  identified by a **URI** such as `registry://policies/naming`;
  `resources/read` fetches one by its URI and returns its `contents`.
- **Prompts:** `prompts/list` returns each prompt's `name` and the
  `arguments` it takes; `prompts/get` fills one in with those arguments
  and returns ready-to-use `messages`.

A tool's `inputSchema` is plain JSON Schema, the same kind of schema
[Lesson 2 generated from a Pydantic model](/03-tool-design-for-agents/02-tool-schemas-and-argument-validation/01-the-schema-generated-the-payoff-completed/#the-full-tool-definition-built-from-code).
Everything from
[Lesson 1 about names and descriptions](/03-tool-design-for-agents/01-designing-tools-a-model-can-use-well/01-names-and-descriptions-as-prompts/#what-a-description-should-say)
applies unchanged: a server's tool descriptions are prompts the model
reads, and a vague one causes the same wrong calls it always did.

</Subsection>

<Subsection title="How a host uses each one" variant="a">

Here's the difference in practice: the same host, handling one of each:

<LiveDemo client:load code={HOST_DEMO} />

- **The tool** is reshaped into the model's tool format and offered.
  Note that MCP spells the schema key `inputSchema` while Claude's API
  expects `input_schema`. The host translates between them, which a
  later lesson in this module does for every tool a server lists.
- **The resource** is read by the host and pasted into the user's
  message, wrapped in a tag so the model can tell the policy apart from
  the request. That's the
  [delimiter technique from Module 2's prompting lesson](/02-the-agent-loop/02-prompting-fundamentals/03-output-format-and-delimiters/#the-fix-delimiters-mark-the-boundary-explicitly),
  used for its original purpose: marking where supplied data begins and
  ends.
- **The prompt** becomes the conversation's opening messages. MCP's
  prompt messages carry `content` as an object with a `type`, so the
  host converts them into whatever message format its model API
  expects.

</Subsection>

<Subsection title="Why agents mostly use tools" variant="b">

In agent systems, tools do most of the work, because they're the only
primitive the model can reach for on its own in the middle of a task.
That's what an agent is:
[the model deciding what to do next](/02-the-agent-loop/01-agents-workflows-and-the-loop/02-agent-vs-workflow-vs-chatbot/#the-distinguishing-question-who-decides-what-happens-next).

Resources and prompts matter more in applications with a person in the
loop, such as a chat app where the user attaches files or picks a task
from a menu. When an agent does need data that lives in a resource, a
common pattern is a small tool that reads it, so the model can fetch it
when it decides it needs it. The rest of this module, like most real
agent code, focuses on tools.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Which primitive does the model itself decide to use, in the middle of a task?",
      options: [
        "Resources",
        "Prompts",
        "All three, equally",
        "Tools",
      ],
      correctIndex: 3,
      explanation:
        "Tools are model-controlled. Resources are chosen by the host application and prompts by a person, so the model never requests either directly.",
    },
    {
      question: "A chat app lets the user attach the registry's naming policy to their message before sending it. Which primitive is that?",
      options: [
        "A resource, read by the host and placed in front of the model",
        "A tool, since the policy comes from a server",
        "A prompt, since the user chose it",
        "A notification from the server",
      ],
      correctIndex: 0,
      explanation:
        "It's data the application reads and includes in the conversation. The user choosing to attach it doesn't make it a prompt; prompts are message templates that start a task.",
    },
    {
      question: "How is a resource identified when a client reads it?",
      options: [
        "By its position in the resources/list result",
        "By its URI, such as registry://policies/naming",
        "By the tool that created it",
        "By a handle returned from server/discover",
      ],
      correctIndex: 1,
      explanation:
        "resources/read takes a uri. Positions in a list can change between calls; the URI is the resource's stable name.",
    },
    {
      question: "What does prompts/get return?",
      options: [
        "A tool the model can call to run the prompt",
        "The prompt's JSON Schema",
        "Filled-in messages, ready to start a conversation",
        "A resource URI to read next",
      ],
      correctIndex: 2,
      explanation:
        "A prompt is a template. Getting it with arguments returns concrete messages, which the host converts into its model API's format.",
    },
    {
      question: "An agent sometimes needs a large reference document that a server exposes as a resource. What's a common way to let the agent use it when it decides to?",
      options: [
        "Paste every resource into every request",
        "Turn it into a prompt the user must pick",
        "Ask the server to send it as a notification",
        "Wrap reading it in a small tool, so the model can fetch it when it needs it",
      ],
      correctIndex: 3,
      explanation:
        "Only tools are model-controlled. A reading tool puts the decision with the model, instead of paying the context cost on every request.",
    },
  ]}
/>

</CheckpointZone>

<CheckpointZone>

<GradedExercise
  client:load
  task={"A host has read a resource with resources/read and wants to place it in front of the model. Implement resource_to_context(read_result), which turns the result's contents into one string. Each content item that has a \"text\" field becomes <resource uri=\"THE_URI\">, then a newline, then THE_TEXT, then a newline, then </resource>. Each item without \"text\" is binary (it has a \"blob\" instead). Don't include the binary data. Put a short placeholder inside the tag that mentions its mimeType, all on one line: <resource uri=\"THE_URI\">[binary content (image/png) not shown]</resource>. Join the blocks with a newline, in the order they appear. An empty contents list gives an empty string."}
  starterCode={STARTER}
  hiddenTests={TESTS}
  hint={"Loop over read_result.get(\"contents\", []), build one string per item into a list, and finish with \"\\n\".join(blocks). \"text\" in item tells you which kind of item you have. For the binary case, item.get(\"mimeType\", \"unknown type\") handles a missing type."}
  correctAnswer={{
    code: REFERENCE,
    explanation:
      "The tags do two jobs. They mark exactly where the resource's text starts and ends, so the model can't confuse it with the user's own words, and they keep each item's URI next to its content, so the model can refer to which document said what. Leaving binary data out matters too: base64 image data is thousands of tokens of meaningless characters to the model, the same cost problem as Lesson 3's oversized tool results.",
  }}
/>

</CheckpointZone>
