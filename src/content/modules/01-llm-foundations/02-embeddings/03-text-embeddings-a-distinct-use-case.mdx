---
title: "Text embeddings — a distinct, separate use case"
lesson: "02-embeddings"
order: 3
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import SentenceEmbeddingSpace from "../../../../components/lesson/SentenceEmbeddingSpace";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="Not the same thing as a token embedding" variant="a">

This is worth stating directly, since the two are easy to conflate
given how similar the word "embedding" makes them sound:
[a token embedding, from the previous concept](/01-llm-foundations/02-embeddings/02-token-embeddings-internal-input-layer/),
is one vector *per token*, entirely internal to a model's own
architecture, never touched directly by an API user. A **text
embedding** is something different in almost every respect: one vector
representing an *entire piece of text* — a sentence, a paragraph, a
whole document — produced by a separate, dedicated embedding model, and
something an API user requests directly, on purpose, as the actual
point of the call.

| | Token embedding | Text embedding |
|---|---|---|
| **Scope** | One per token | One per whole piece of text |
| **Where it lives** | Internal to a model's own architecture | Requested directly via an API call |
| **Who touches it** | Nobody outside the model itself | The API user, directly |
| **Purpose** | The model's own entry-point representation | Comparing meaning across texts |

</Subsection>

<Subsection title="What a text embedding model actually does" variant="b">

A text embedding model takes a whole piece of text as input and
returns a single vector — hundreds of numbers — meant to capture that
text's overall meaning, positioned in
[the same kind of meaning-space from the opening concept](/01-llm-foundations/02-embeddings/01-what-an-embedding-actually-is/),
just for whole texts instead of individual words:

```python
# illustrative — not a real embedding model, just showing the shape
def text_to_embedding(text: str) -> list:
    ...  # a real embedding model, not implemented here

sentence_a = text_to_embedding("The cat sat on the mat.")
sentence_b = text_to_embedding("A feline was resting on the rug.")
sentence_c = text_to_embedding("The stock market fell sharply today.")
```
*(illustrative — a real embedding model's actual output isn't shown
here; the point is the shape of the operation, not a specific result)*

`sentence_a` and `sentence_b` share almost no words in common at all —
`"cat"` versus `"feline"`, `"mat"` versus `"rug"` — but they mean nearly
the same thing, and a good text embedding model produces vectors for
them that land close together in the embedding space anyway.
`sentence_c` means something completely unrelated, and its vector lands
far from both. This is the actual payoff: text embeddings let you
compare meaning directly, not exact wording — something plain keyword
matching could never do, since `"cat"` and `"feline"` share no letters
at all.

Below is a real sentence-embedding model's own output for a small bank
of sentences — not the toy `text_to_embedding` shown above, but an
actual model's real vectors, PCA-projected to 2D the same way Concept
1's word space was. Check a few boxes and see for yourself: sentences
that mean the same thing land together even with completely different
wording, and sentences about unrelated topics land far apart.

<SentenceEmbeddingSpace client:load />

</Subsection>

<Subsection title="Why this matters — a preview, not the full picture yet" variant="a">

Being able to compare pieces of text by meaning rather than exact
wording is the foundational operation a whole later part of this course
is built on: finding the most *relevant* piece of text to a question,
out of a large collection, by comparing embeddings rather than searching
for matching keywords. That's the actual groundwork this concept exists
to lay — the full mechanism (storing many text embeddings, searching
across them efficiently, and using the results to inform an LLM's
answer) is the RAG Systems module's job, not this lesson's. What matters
here is understanding what a text embedding actually is and what
comparing two of them can tell you — covered precisely, with the actual
math, next.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "What's the key difference between a token embedding and a text embedding?",
      options: [
        "They're the same thing, just different names for it",
        "A token embedding is one vector per token, internal to a model's architecture; a text embedding is one vector for an entire piece of text, requested directly via an API call",
        "Text embeddings are only used for very short pieces of text",
        "Token embeddings are requested by API users; text embeddings are internal",
      ],
      correctIndex: 1,
      explanation:
        "Token embeddings are per-token and purely internal; text embeddings are per-whole-text and explicitly requested by an API caller — opposite in scope and in who touches them.",
    },
    {
      question:
        'Why can two sentences with almost no words in common — like "The cat sat on the mat" and "A feline was resting on the rug" — end up with similar text embeddings?',
      options: [
        "They can't — shared wording is required for similar embeddings",
        "A text embedding captures overall meaning, not exact wording — two sentences meaning nearly the same thing can land close together in embedding space even sharing almost no words",
        "The embedding model checks a thesaurus before comparing sentences",
        "Similar sentence length alone determines embedding similarity",
      ],
      correctIndex: 1,
      explanation:
        "A text embedding is built to capture meaning, not surface wording — that's exactly why two near-paraphrases with almost no shared vocabulary still land close together.",
    },
    {
      question: "What real capability does comparing text embeddings enable that plain keyword matching can't?",
      options: [
        "Finding text that's similar in meaning, even when the actual wording is completely different",
        "Faster searching, with no difference in what gets found",
        "Checking whether two pieces of text are byte-for-byte identical",
        "Automatically translating text between languages",
      ],
      correctIndex: 0,
      explanation:
        "Keyword matching can only find shared words — comparing embeddings finds shared meaning, which is a fundamentally different and more useful capability for most real search needs.",
    },
    {
      question: "What does this concept explicitly leave for a later module to cover?",
      options: [
        "What a text embedding actually is",
        "The full mechanism of storing and searching across many text embeddings to answer a question — the actual implementation of a RAG pipeline",
        "The difference between token and text embeddings",
        "Nothing — this concept covers the complete RAG pipeline already",
      ],
      correctIndex: 1,
      explanation:
        "This concept lays the conceptual groundwork (what a text embedding is, what comparing two tells you) — the full storage-and-search pipeline built on top of that is the RAG Systems module's job.",
    },
  ]}
/>

</CheckpointZone>
