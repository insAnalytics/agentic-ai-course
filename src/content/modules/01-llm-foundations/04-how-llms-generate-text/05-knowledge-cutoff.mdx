---
title: "Knowledge cutoff"
lesson: "04-how-llms-generate-text"
order: 5
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="Training ends, and the parameters stop changing" variant="a">

A model's parameters —
[the embeddings, the attention weights, everything covered across this module so far](/01-llm-foundations/03-attention-and-transformer-architecture/00-intro/) —
are learned entirely from training data collected up to some specific
point in time, and once training finishes, those parameters are fixed.
Nothing about them changes during actual use — a conversation with the
model doesn't retrain it, doesn't update its weights, doesn't teach it
anything new that persists afterward. Whatever happened in the world
*after* that training data was collected simply never had any
opportunity to influence the model's parameters at all. This cutoff
point is the model's **knowledge cutoff**.

</Subsection>

<Subsection title="Why asking about something recent looks structurally identical to hallucination" variant="b">

Ask a model about an event from after its knowledge cutoff, and
something specific happens mechanically: there's genuinely no training
data reflecting that event anywhere in the parameters that produced this
model, so the exact same logit-computation process from earlier in this
lesson runs with nothing real to actually draw on —
[structurally identical to the fabricated-city case from the earlier concept](/01-llm-foundations/04-how-llms-generate-text/03-the-model-predicts-it-doesnt-know/),
and capable of producing exactly the same kind of confident-sounding,
plausible, fabricated answer covered in
[the hallucination concept](/01-llm-foundations/04-how-llms-generate-text/04-hallucination-as-a-direct-consequence/).
A model well-trained to recognize this specific situation might instead
produce something like "I don't have information about that" — but that
response itself is also just a learned pattern the model was trained to
produce when a question's phrasing resembles ones it was taught to
decline, not some separate, built-in awareness of exactly where its own
knowledge actually ends.

Worth one more nuance: the cutoff isn't a perfectly sharp line in
practice. Training data isn't uniformly collected or cleanly
time-stamped, so a model's knowledge right around its cutoff date is
often inconsistent — some things from just before the cutoff might be
thin or contradictory, and a model doesn't necessarily know its own
cutoff date precisely either, since that's just another fact it would
need to have learned during training like anything else.

</Subsection>

<Subsection title="The fix, within a single conversation" variant="a">

None of this means a model can never discuss anything recent — it means
it can't do so from its own trained-in parameters alone. Providing
genuinely current information directly *in the prompt*, as part of the
input rather than relying on what's baked into the weights, largely
sidesteps the problem for that one conversation — the model can reason
over information handed to it right now, even if that information
postdates its training. "Largely", because provided context isn't
guaranteed to win: a model can still misread it, overlook it in a long
input, or lean on a strongly learned older fact instead, which is why
grounded systems are also evaluated, not just built. This is the natural entry point into two things
covered properly later in this course: retrieval — finding and
injecting relevant, current information into context, the actual
mechanism behind RAG — and tool use — letting a model call a live data
source directly rather than relying on anything memorized. Both are
genuine fixes; neither is implemented here — this lesson stops at
understanding *why* the cutoff exists and *why* it matters, the same
scope boundary held throughout this lesson.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Why does a model's knowledge cutoff exist at all?",
      options: [
        "It's an arbitrary restriction added after training, for safety reasons",
        "The model's parameters are fixed once training finishes, and were learned only from data collected up to that point — nothing after that point could have influenced them",
        "Models are retrained after every conversation to update their cutoff",
        "The cutoff only applies to certain topics, not the model as a whole",
      ],
      correctIndex: 1,
      explanation:
        "The cutoff is a direct consequence of training ending and parameters freezing — nothing that happened afterward could possibly have shaped those fixed weights.",
    },
    {
      question:
        "Why does asking a model about an event after its knowledge cutoff carry the same risk as the earlier fabricated-city example?",
      options: [
        "It doesn't — recent-event questions are handled by a completely separate mechanism",
        "There's genuinely no training data reflecting that event, so the same generation process runs with nothing real to draw on, capable of producing the same kind of confident, plausible fabrication",
        "Models automatically refuse to answer any question about recent events",
        "Knowledge cutoff only affects questions about specific dates, never events",
      ],
      correctIndex: 1,
      explanation:
        "Both cases run the identical logit-then-softmax process with nothing real to ground the answer in — a post-cutoff event and a fabricated scenario look mechanically the same from inside generation.",
    },
    {
      question: "Does a model reliably know its own exact knowledge cutoff date?",
      options: [
        "Yes, always, with complete precision",
        "Not necessarily — its awareness of its own cutoff is itself just another learned pattern from training, not some separate, built-in certainty",
        "Models never have any awareness of their own cutoff at all",
        "The cutoff date is recalculated in real time during each conversation",
      ],
      correctIndex: 1,
      explanation:
        "A model's sense of its own cutoff is just another thing it had to learn during training, like any other fact — not a separate, guaranteed-accurate self-awareness mechanism.",
    },
    {
      question: "How does providing current information directly in a prompt address the knowledge cutoff problem?",
      options: [
        "It permanently updates the model's trained parameters",
        "It lets the model reason over information handed to it right now, sidestepping the need to have that information baked into its trained-in weights",
        "It doesn't help — cutoff-related limitations can't be worked around at all",
        "It only works for information from before the cutoff date",
      ],
      correctIndex: 1,
      explanation:
        "Nothing about the model's weights changes — the fix works entirely within one conversation, by handing current information in as input the model can reason over directly.",
    },
  ]}
/>

</CheckpointZone>
