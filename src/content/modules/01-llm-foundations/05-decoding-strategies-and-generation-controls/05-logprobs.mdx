---
title: "Logprobs — inspecting how confident the model actually was"
lesson: "05-decoding-strategies-and-generation-controls"
order: 5
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import LiveDemo from "../../../../components/lesson/LiveDemo";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="The signal Lesson 4 pointed at, now a real tool" variant="a">

[Lesson 4's closing synthesis noted](/01-llm-foundations/04-how-llms-generate-text/06-recap-practice/#closing-synthesis-reason-it-through-then-check)
that a distribution's *shape* — one dominant candidate versus several
closely competing ones — often reveals more about how settled an answer
actually is than the fluency of the final generated text ever does.
**Logprobs** are the actual, practical way to access that signal
directly: some APIs (OpenAI's and Gemini's, for example) can return,
alongside the generated text itself, the exact probability the model
assigned to each token it chose. Not every provider exposes them:
Anthropic's API, for one, doesn't return logprobs.

</Subsection>

<Subsection title="Why log probability, specifically" variant="b">

A logprob is the natural logarithm of a token's probability, not the
raw probability itself. The reason comes down to how probabilities
combine across a sequence: the probability of an entire generated
sequence is the *product* of every individual token's probability —
multiplying many numbers, each already less than `1`, together
repeatedly produces extremely tiny numbers very quickly, awkward to
work with and prone to numerical precision problems. Taking the
logarithm converts that multiplication into simple addition
(`log(a × b) = log(a) + log(b)`), which is both numerically far more
stable and much easier to actually compute with across a long sequence.

</Subsection>

<Subsection title="Reading a logprob directly" variant="a">

Since probability is always between `0` and `1`, its logarithm is
always zero or negative — `log(1) = 0`, and anything below `1` produces
a negative number, getting more negative the closer the original
probability was to `0`. This gives a simple reading rule: a logprob
close to `0` means high confidence; a strongly negative logprob means
low confidence, even for the token that ultimately "won."

<LiveDemo client:load code={`import math

def softmax(logits: dict) -> dict:
    exp_values = {token: math.exp(score) for token, score in logits.items()}
    total = sum(exp_values.values())
    return {token: value / total for token, value in exp_values.items()}

def to_logprobs(probabilities: dict) -> dict:
    return {token: round(math.log(prob), 3) for token, prob in probabilities.items()}

confident_distribution = softmax({"Paris": 8.9, "Lyon": 1.2, "Marseille": 0.8})
uncertain_distribution = softmax({"Aldric Thorne": 6.1, "Marcus Reed": 5.9, "Elena Vasquez": 5.3})

print(to_logprobs(confident_distribution))
print(to_logprobs(uncertain_distribution))`} />

These are
[the exact confident-fact and fabricated-scenario distributions from Lesson 4](/01-llm-foundations/04-how-llms-generate-text/03-the-model-predicts-it-doesnt-know/#the-same-mechanism-whether-the-answer-is-grounded-or-not),
run through the same `softmax` first. `"Paris"`'s logprob, `-0.001`,
sits right next to `0` — genuine, strong confidence. `"Aldric Thorne"`'s
logprob, `-0.819`, is meaningfully more negative — the model "won" that
token over its competitors, but with far less certainty than `"Paris"`
had, a distinction the generated text alone (a fluently stated name,
either way) would never reveal.

</Subsection>

<Subsection title="Why this is practically useful" variant="b">

A generated response reads equally fluent whether the model was highly
confident or barely edged out a close competitor — that's
[precisely what made hallucination hard to spot from the text alone, back in Lesson 4](/01-llm-foundations/04-how-llms-generate-text/04-hallucination-as-a-direct-consequence/#not-a-malfunction-the-same-mechanism-landing-on-a-wrong-answer).
Logprobs give a way to check, after generation, whether a specific claim
came from a strongly confident distribution or a much closer call — a
genuinely useful signal for flagging outputs worth double-checking, even
though this lesson stops short of covering how that signal gets used in
a real evaluation or confidence-scoring pipeline, which belongs to later
parts of this course.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "What is a logprob, mathematically?",
      options: [
        "The raw probability itself, unmodified",
        "The natural logarithm of a token's probability",
        "The number of times a token appeared in training data",
        "A count of how many tokens were considered before this one",
      ],
      correctIndex: 1,
      explanation:
        "A logprob is math.log(probability) — always zero or negative, since probabilities never exceed 1.",
    },
    {
      question: "Why is log probability preferred over raw probability when working with an entire generated sequence?",
      options: [
        "Log values are always positive, which is easier to display",
        "A sequence's overall probability is the product of many individual probabilities, and taking logs converts that multiplication into addition, which is numerically more stable",
        "Raw probabilities can't be computed for sequences longer than one token",
        "Logprobs are only used for very short sequences",
      ],
      correctIndex: 1,
      explanation:
        "Multiplying many numbers below 1 collapses toward zero fast; log(a × b) = log(a) + log(b) turns that into stable addition.",
    },
    {
      question: "What does a logprob close to 0 indicate, compared to a strongly negative logprob?",
      options: [
        "A logprob close to 0 indicates low confidence; strongly negative indicates high confidence",
        "A logprob close to 0 indicates high confidence (probability near 1); a strongly negative logprob indicates lower confidence",
        "Logprob values have no relationship to confidence at all",
        "Only positive logprob values are ever possible",
      ],
      correctIndex: 1,
      explanation:
        "log(1) = 0, so a logprob near 0 means probability near 1; the closer the probability gets to 0, the more negative its logprob becomes.",
    },
    {
      question: 'Why does "Aldric Thorne"\'s logprob reveal something the generated text itself wouldn\'t?',
      options: [
        "It doesn't — fluent text and logprobs always convey identical information",
        "The generated name reads equally fluent regardless of confidence; the meaningfully negative logprob reveals that this token only narrowly beat its competitors, unlike a case with much stronger confidence",
        '"Aldric Thorne" is grammatically incorrect, which the logprob detects',
        "Logprobs can only be computed for real, factual answers",
      ],
      correctIndex: 1,
      explanation:
        "The text reads just as fluent either way; the logprob (-0.819, vs. -0.001 for \"Paris\") exposes how much closer that contest actually was.",
    },
  ]}
/>

</CheckpointZone>
