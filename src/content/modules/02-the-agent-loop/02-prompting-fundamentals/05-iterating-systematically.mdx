---
title: "Iterating systematically rather than by vibes"
lesson: "02-prompting-fundamentals"
order: 5
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

No new prompting technique here — everything below reuses the previous
four concepts as-is. What's new is a *discipline* for actually improving a
prompt during development: check a small set of representative cases
together, not one. This is also deliberately *not* formal evaluation —
scoring metrics, benchmarking, and statistical rigor belong to a later
module on evaluation; this concept stays at the level of a practical
development habit.

<Subsection title="The pain: judging a change from a single example" variant="a">

A common, unsystematic pattern: tweak a prompt's wording, run it once,
look at the one resulting output, and decide by gut feeling whether the
change "helped." This is a genuinely weak signal — a model's output can
vary somewhat from run to run even for the same prompt, so one example
looking better or worse after a change might just be noise, not a real
improvement or regression at all.

</Subsection>

<Subsection title="The fix: a small set of representative cases, checked together" variant="b">

```
Test set for the "Name (Role)" extraction prompt from the examples-in-the-prompt concept:

1. "Dr. Sarah Chen, who leads the research division, announced..."
   (a clear, simple case)
2. "The keynote was delivered by Marcus Webb, Senior Vice President of
   Global Operations and Strategic Partnerships."
   (a longer, more complex title)
3. "Elena Ruiz spoke about the upcoming product launch."
   (no role explicitly stated at all)

Illustrative results, Prompt Version A:
1. "Sarah Chen (Research Division Lead)" — correct
2. "Marcus Webb (Senior Vice President of Global Operations and
   Strategic Partnerships)" — correct, but awkwardly long
3. "Elena Ruiz (Unknown)" — arguably reasonable, but never specified
   how to handle a missing role
```

(The results are illustrative, not fetched from a real call.)

Checking *only* case 1 would have suggested Prompt A works well — it's the
easy case, and it succeeds cleanly. Checking all three together reveals
real, specific gaps: no instruction for handling a long title gracefully,
and no defined behavior for a missing role at all. This is exactly the
value of a representative set: it surfaces problems a single lucky (or
unlucky) example would never reveal either way.

</Subsection>

<Subsection title="What makes a set actually representative" variant="a">

The same principle from
[the earlier discussion of representative few-shot examples](/02-the-agent-loop/02-prompting-fundamentals/02-examples-in-the-prompt/#representative-examples-not-just-more-examples)
applies here directly: a good test set covers the genuine range of real
input variation — different lengths, real edge cases, different
phrasings — not several near-identical easy cases that would only ever
produce a false sense of confidence. Iterating against three cases that
are all essentially the same case restated doesn't actually tell you much
more than iterating against one.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Why is judging a prompt change from a single example output a genuinely weak signal?",
      options: [
        "A single example is always sufficient — this concern doesn't actually apply",
        "A model's output can vary somewhat from run to run for the same prompt, so one example looking better or worse might just be noise, not a real change",
        "Single examples are always misleading, in every case, with no exceptions",
        "This is only a concern for very long prompts",
      ],
      correctIndex: 1,
      explanation:
        "One output can look better or worse by chance. \"Always misleading\" overshoots — a single example is weak evidence, not worthless.",
    },
    {
      question: "What did checking all three test cases together reveal, that checking only the first case wouldn't have?",
      options: [
        "Nothing — all three cases produced identical results",
        "Real, specific gaps — no instruction for a long title, and no defined behavior for a missing role — that the easy first case alone never surfaced",
        "That the prompt should be discarded entirely and rewritten from scratch",
        "That few-shot examples don't actually help with this task",
      ],
      correctIndex: 1,
      explanation:
        "Case 1 was the easy case and passed cleanly. The long title and the missing role are what exposed the prompt's gaps.",
    },
    {
      question: "Why does this concept explicitly distinguish itself from formal evaluation methodology?",
      options: [
        "Formal evaluation and this practice are exactly the same thing",
        "This concept stays at the level of a practical development habit — checking a few representative cases together — while scoring metrics and statistical rigor belong to a later, dedicated module",
        "Formal evaluation is never actually necessary for prompt development",
        "This concept teaches complete benchmarking methodology directly",
      ],
      correctIndex: 1,
      explanation:
        "Three hand-checked cases is a development habit, not measurement. Metrics and rigor come later, and they do matter.",
    },
    {
      question: "Why would a test set of three near-identical, equally easy cases fail to be genuinely useful for iterating on a prompt?",
      options: [
        "It wouldn't fail — any three cases are equally useful regardless of similarity",
        "It would give a false sense of confidence, the same way a narrow, non-representative few-shot example set can mislead — real variation needs to actually be covered to surface real gaps",
        "Three cases are never enough, regardless of their content",
        "Near-identical cases always produce completely different results",
      ],
      correctIndex: 1,
      explanation:
        "Three copies of the same easy case are effectively one case. The variety, not the count, is what surfaces problems.",
    },
  ]}
/>

</CheckpointZone>
