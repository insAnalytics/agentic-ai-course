---
title: "What control was given up"
lesson: "11-from-hand-rolled-to-a-runtime"
order: 3
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="Explicit becomes implicit — not gone, just hidden" variant="a">

Everything
[the framework version from earlier in this lesson](/02-the-agent-loop/11-from-hand-rolled-to-a-runtime/02-rebuilding-in-langchain/)
does, the hand-rolled version also did — but several things that were
explicit and inspectable by hand are now internal to the framework.
Most of what follows is genuinely still the same mechanism, just no
longer visible; one is worse than that — a case where adopting the
framework's default actually changes the behavior itself, not just how
much of it you can see:

- **The step cap doesn't just stop being visible — it stops behaving
  the same way.**
  [`recursion_limit`, from the previous concept](/02-the-agent-loop/11-from-hand-rolled-to-a-runtime/02-rebuilding-in-langchain/),
  counts in a different unit than
  [Lesson 6's `max_steps`](/02-the-agent-loop/06-termination-failure-and-control/02-max-steps/#the-simplest-possible-guard-a-hard-cap-unconditionally)
  did (roughly half as many loop iterations as the number suggests),
  and it fails differently too — `GraphRecursionError` crashes the run,
  where `max_steps` was built specifically to avoid that and return a
  clean message instead. Reaching for the default without knowing this
  isn't a visibility problem to shrug off; it's a real behavior
  regression a framework's convenient default can quietly introduce,
  exactly why `ModelCallLimitMiddleware`'s `exit_behavior="end"` exists
  as the closer match — but nothing forces you to reach for it if you
  don't already know `recursion_limit` alone isn't equivalent.
- **Exactly how state serializes** —
  [Lesson 7's `to_dict()`/`serialize_messages`](/02-the-agent-loop/07-agent-state-and-the-scratchpad/02-serializing-state/)
  happens somewhere inside `agent.invoke()`, not in code you wrote or
  can trace line by line.
- **Exactly what the dispatch mechanism does** on an unexpected input —
  [Lesson 6's `execute_tool_safely`](/02-the-agent-loop/06-termination-failure-and-control/07-recap-practice/)
  was fully yours to read; a framework's equivalent is a black box
  unless you go looking for its source.

This isn't a criticism of frameworks — it's the actual, honest trade
[this course has named repeatedly](/02-the-agent-loop/08-planning-and-decomposition/04-plan-and-execute-vs-reactive/):
less code to write, at the cost of less direct visibility into exactly
what's happening. Knowing this loop's real mechanics by having built it
yourself is what makes that trade a genuine choice, not a leap of
faith.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: 'What does this concept mean by "explicit becomes implicit"?',
      options: [
        "Frameworks remove capabilities the hand-rolled version had",
        "Mechanisms like termination logic and serialization still happen, but internally, no longer as code you wrote and can read directly",
        "Frameworks are strictly worse than hand-rolled code",
        "Nothing actually changes between the two versions",
      ],
      correctIndex: 1,
      explanation:
        "The mechanisms themselves are unchanged -- a cap still fires, state still serializes, tools still get dispatched. What changes is whether that logic is code you wrote and can read, or something happening inside the framework.",
    },
    {
      question: "Why does this concept frame understanding the hand-rolled mechanics as valuable, even once a framework is adopted?",
      options: [
        "It isn't valuable -- the framework makes that knowledge obsolete",
        "It's what makes adopting a framework a genuine, informed choice about trading code for convenience, rather than a leap of faith into something never actually understood",
        "Frameworks require rewriting the hand-rolled version every time",
        "This concept doesn't actually make that argument",
      ],
      correctIndex: 1,
      explanation:
        "Choosing convenience over visibility is only a real trade-off if you understand what you're giving up. Having built the loop by hand is what turns adopting a framework into an informed decision.",
    },
    {
      question: "Why is recursion_limit a genuinely different case from the other examples in this concept, not just another thing that's now hidden?",
      options: [
        "It isn't different -- all of these examples are the same kind of thing",
        "The other examples are the same mechanism, just less visible; recursion_limit's default actually behaves differently from max_steps -- a different unit, and a crash instead of a clean stop -- so reaching for it without knowing that is a real behavior regression, not just reduced visibility",
        "recursion_limit doesn't actually do anything at all",
        "max_steps and recursion_limit are identical in every respect",
      ],
      correctIndex: 1,
      explanation:
        "Serialization and dispatch still work the same way under the framework, just out of view. recursion_limit alone doesn't reproduce max_steps's behavior at all -- it's a case where the convenient default is a genuine downgrade, not merely a hidden version of the same guard.",
    },
  ]}
/>

</CheckpointZone>
