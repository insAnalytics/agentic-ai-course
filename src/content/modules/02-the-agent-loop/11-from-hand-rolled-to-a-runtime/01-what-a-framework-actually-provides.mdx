---
title: "What a framework actually provides"
lesson: "11-from-hand-rolled-to-a-runtime"
order: 1
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="Four things, two already built by hand" variant="a">

A framework packages the same core pieces this module built from
scratch, plus two new ones:

- **The loop** —
  [Lesson 4's `while`/dispatch structure](/02-the-agent-loop/04-writing-the-loop-by-hand/02-from-round-trip-to-loop/#the-actual-loop-written-and-running-for-real),
  now hidden behind a method call.
- **State handling** —
  [Lesson 7's scratchpad and serialization](/02-the-agent-loop/07-agent-state-and-the-scratchpad/00-intro/),
  managed internally rather than a `messages` list you build yourself.
- **Tracing hooks** — new: structured visibility into what the loop
  actually did at each step, without hand-adding `print()` statements
  everywhere the way this module's demos have throughout.
- **A deployment path** — new: a defined way to actually run this agent
  as a real, reachable service, beyond a local script. Kept as a
  forward-pointer here — full deployment technique belongs to a much
  later module.

None of this is new capability the hand-rolled version couldn't do —
it's the same mechanisms, packaged and given a stable, reusable API.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Which two things a framework provides has this module already built by hand?",
      options: [
        "Tracing hooks and a deployment path",
        "The loop (Lesson 4) and state handling (Lesson 7)",
        "Nothing -- frameworks provide entirely new capabilities",
        "Only the loop, nothing else",
      ],
      correctIndex: 1,
      explanation:
        "The while/dispatch loop and the scratchpad/serialization mechanics were both built from scratch earlier in this module. A framework packages the same mechanisms, not new capability.",
    },
    {
      question: "What do tracing hooks add that this module's demos relied on instead?",
      options: [
        "Nothing -- tracing and printing are identical",
        "Structured visibility into the loop's behavior, without hand-adding print() statements at every step",
        "A way to avoid ever needing to debug an agent",
        "A replacement for max_steps",
      ],
      correctIndex: 1,
      explanation:
        "Every demo so far has used print() at specific points to show what's happening. Tracing hooks give that same visibility in a structured way, built into the framework rather than hand-added each time.",
    },
  ]}
/>

</CheckpointZone>
