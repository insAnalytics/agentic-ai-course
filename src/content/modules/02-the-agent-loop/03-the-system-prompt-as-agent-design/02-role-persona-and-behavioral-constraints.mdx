---
title: "Role, persona, and behavioral constraints"
lesson: "03-the-system-prompt-as-agent-design"
order: 2
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

No new prompting mechanism here — a system prompt is just a prompt, so
[every technique from Lesson 2](/02-the-agent-loop/02-prompting-fundamentals/00-intro/)
already applies to it directly. This concept is about applying that
existing technique to this specific, high-stakes use case.

<Subsection title="The pain: a role-less system prompt" variant="a">

```
System prompt: "You are a helpful assistant."
```

This is
[exactly the kind of vagueness Lesson 2 opened with](/02-the-agent-loop/02-prompting-fundamentals/01-specificity-and-clear-instructions/#the-pain-a-vague-instruction-leaves-too-much-to-guess),
now sitting in the one place it matters most — the instructions that
apply across an agent's *entire* interaction, not just one response. With
no defined scope, no constraints, and no guidance on what to do when
information isn't actually known, the agent has no particular reason to
stay on-topic, or to avoid
[producing a fluent, confident-sounding answer it doesn't actually have grounding for](/01-llm-foundations/04-how-llms-generate-text/04-hallucination-as-a-direct-consequence/)
— hallucination isn't a special malfunction here; it's the default
behavior of a system with no instruction telling it otherwise.

</Subsection>

<Subsection title="Role, and explicit behavioral constraints" variant="b">

```
System prompt: "You are a customer support agent for Riverside Books,
a small independent bookstore. Always mention the specific price when
discussing a book. Never claim a book is in stock unless you've
actually checked using the check_inventory tool. If you don't know
something, say so clearly rather than guessing."
```

- **Role** — `"a customer support agent for Riverside Books"` frames who
  the agent is acting as, narrowing tone and scope directly,
  [the same kind of specificity from Lesson 2](/02-the-agent-loop/02-prompting-fundamentals/01-specificity-and-clear-instructions/#the-fix-specify-what-how-and-the-constraints),
  just establishing a persona rather than a one-off task shape.
- **Constraints** — explicit always/never instructions: always cite the
  price, never claim stock without actually checking. These are the real,
  concrete guardrails against the exact failure mode named above — a
  model with no instruction against guessing has no particular reason not
  to; one explicitly told to defer to a real check, and to admit
  uncertainty plainly, has a genuine behavioral target to aim for
  instead.

As with any prompt, these constraints are a strong nudge, not a mechanical
guarantee — they make the right behavior much more likely, not certain,
which is why a system prompt is one layer of a reliable agent rather than
the whole of it.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Why does a vague, role-less system prompt increase the risk of ungrounded, confidently-stated claims?",
      options: [
        "Vague system prompts always cause the model to refuse to answer",
        "With no instruction constraining it, the model has no particular reason not to produce a fluent, plausible-sounding answer it doesn't actually have grounding for — the default behavior of an unconstrained system",
        "This risk is entirely unrelated to how the system prompt is written",
        "Vague system prompts only affect response length, never accuracy",
      ],
      correctIndex: 1,
      explanation:
        "Fluent, plausible text is what generation produces by default. Nothing in a bare \"helpful assistant\" prompt tells the model to do anything else when it doesn't know.",
    },
    {
      question: "What does defining a role in a system prompt actually do, mechanically?",
      options: [
        "It switches the model to a separate set of weights fine-tuned for that role",
        "It's a form of specificity, narrowing tone and scope directly, the same underlying technique from Lesson 2 applied to a persistent persona instead of a one-off task",
        "It requires a completely different mechanism than anything covered in Lesson 2",
        "It only affects the model's vocabulary, never its actual behavior",
      ],
      correctIndex: 1,
      explanation:
        "A role is just a specific instruction that holds for the whole interaction. There's no new mechanism, only the same technique in a standing position.",
    },
    {
      question: 'Why is "Never claim a book is in stock unless you\'ve actually checked using the check_inventory tool" a genuinely useful constraint to include?',
      options: [
        "Without it the model couldn't call check_inventory at all, since a tool must be named in the system prompt",
        "It gives the model an explicit behavioral target — defer to a real check rather than guess — directly countering the default risk of a confidently-stated but ungrounded claim",
        "This constraint is redundant with what the model already does by default",
        "Tools automatically prevent any claim not based on their results, regardless of the system prompt",
      ],
      correctIndex: 1,
      explanation:
        "Nothing stops a model from stating stock it never checked unless it's been told not to. Tools provide a real check, but only if the model is directed to use it.",
    },
  ]}
/>

</CheckpointZone>
