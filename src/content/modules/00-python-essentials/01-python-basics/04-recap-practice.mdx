---
title: "Recap & Practice"
lesson: "01-python-basics"
order: 4
---
import QuizGroup from "../../../../components/lesson/QuizGroup";
import GradedExercise from "../../../../components/lesson/GradedExercise";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question:
        "A teammate clones your repo and immediately gets ModuleNotFoundError when running your script, even though it works fine for you. What two things from this lesson are the most likely fix?",
      options: [
        "Reinstall Python entirely",
        "Share a requirements.txt and make sure they're running inside their own activated venv",
        "Send them your .venv folder directly",
        "Switch them to a different OS",
      ],
      correctIndex: 1,
      explanation:
        "Installed packages live in the venv, not the code — a requirements.txt plus an activated venv on their end is what makes the environment reproducible.",
    },
    {
      question: 'You write `if temp == 20 print("warm")` (no colon). What happens?',
      options: [
        "It runs fine, colons are optional",
        "SyntaxError — the colon that opens the block is required",
        "It runs but never prints",
        "IndentationError",
      ],
      correctIndex: 1,
      explanation:
        "The colon is what tells Python a block is starting — leaving it out is a syntax error, not a style issue.",
    },
    {
      question: "In `for tool in tools:`, what would you call `tools` itself?",
      options: ["A counter", "An iterable — something that can hand out its items one at a time", "A function", "An exception type"],
      correctIndex: 1,
      explanation:
        "An iterable is anything that can hand out its items one at a time — a list, a string, a range — and for works the same way with any of them.",
    },
    {
      question: "Why might a bare except: clause hide a real bug rather than handle it safely?",
      options: [
        "It doesn't compile",
        "It catches every error type indiscriminately, including ones you didn't anticipate or intend to handle",
        "It only works with ZeroDivisionError",
        "It's identical to not using try/except at all",
      ],
      correctIndex: 1,
      explanation:
        "A bare except: catches every error type indiscriminately — including ones you didn't anticipate — masking what actually went wrong.",
    },
    {
      question:
        "You have except ArithmeticError: listed before except ZeroDivisionError: in the same try. A division by zero happens. Which block actually runs, and why?",
      options: [
        "ZeroDivisionError's block, since it's more specific",
        "ArithmeticError's block — except clauses are checked top to bottom, and it matches first since ZeroDivisionError is a subclass of it",
        "Both blocks run",
        "Neither — it crashes instead",
      ],
      correctIndex: 1,
      explanation:
        "ZeroDivisionError is a subclass of ArithmeticError, so a broader except listed first will catch it before a more specific one below ever gets checked.",
    },
    {
      question:
        "What's the practical difference between reading a traceback in Python versus getting a compiler error in a language like Java?",
      options: [
        "There is no difference",
        "A compiler error can be caught before the program runs at all; a Python traceback only appears once execution actually reaches the failing line",
        "Python tracebacks never show the actual error type",
        "Java doesn't have error messages",
      ],
      correctIndex: 1,
      explanation:
        "Python has no separate compile step — it only discovers a problem when execution actually reaches that line, and shows you via a traceback, whereas a compiled language can catch structural errors before the program ever runs.",
    },
    {
      question: "Where should a finally block be used?",
      options: [
        "Only when you expect an exception",
        "For cleanup that must happen whether or not an exception occurred (e.g. closing a file)",
        "As a replacement for except",
        "Only inside loops",
      ],
      correctIndex: 1,
      explanation:
        "finally is guaranteed to run regardless of what happened in try/except, making it the right place for cleanup that must always happen.",
    },
  ]}
/>

</CheckpointZone>

<CheckpointZone>

<GradedExercise
  client:load
  task={'Combine the routing logic from Concept 2 with the error handling from Concept 3 — loop through `messages`, classify each one (or catch the case where it isn\'t a string), keep a running count for each category, and return all five counts. `return a, b, c` is valid Python — it returns multiple values at once, separated by commas.'}
  starterCode={`def summarize_requests(messages):
    """
    messages: a list that may contain strings and invalid entries
    (e.g. numbers, None).

    For each item:
      - if it's a valid string, route it using the same rules as
        route_request() earlier in this lesson: "calculate" -> calculator_tool,
        "search" -> search_tool, "remember" -> memory_tool, else -> no_tool
      - if it's not a string at all, count it as invalid instead

    Count how many messages fell into each of the five categories, and
    return the five counts in this order:
    (calculator_count, search_count, memory_count, no_tool_count, invalid_count)
    """
    # TODO: implement using the control flow and error handling from this lesson
    pass`}
  hiddenTests={[
    `result = summarize_requests(["calculate 2+2", "search for cats", "remember my name", "hello", 42, None])
assert result == (1, 1, 1, 1, 2)`,
    "assert summarize_requests([]) == (0, 0, 0, 0, 0)",
    "assert summarize_requests(['hello', 'hi']) == (0, 0, 0, 2, 0)",
  ]}
  hint="Start all five counters at 0. Reuse the route_request-style if/elif chain inside the loop, wrap it in try/except TypeError, and increment the matching counter with += 1 — [the same pattern used for attempts in the while loop example earlier in this lesson](/00-python-essentials/01-python-basics/02-control-flow-and-syntax/#while-loops-and-no)."
  correctAnswer={{
    code: `def summarize_requests(messages):
    calculator_count = 0
    search_count = 0
    memory_count = 0
    no_tool_count = 0
    invalid_count = 0

    for message in messages:
        try:
            if "calculate" in message:
                calculator_count += 1
            elif "search" in message:
                search_count += 1
            elif "remember" in message:
                memory_count += 1
            else:
                no_tool_count += 1
        except TypeError:          # message wasn't a string at all
            invalid_count += 1

    return calculator_count, search_count, memory_count, no_tool_count, invalid_count`,
    explanation:
      "This combines every concept in the lesson: [the for loop and elif chain](/00-python-essentials/01-python-basics/02-control-flow-and-syntax/) from Concept 2, [try/except](/00-python-essentials/01-python-basics/03-error-handling/) from Concept 3, and += counting — all already-taught pieces, just composed together, which is why this is the comprehensive exercise rather than an earlier applied one.",
  }}
/>

</CheckpointZone>
