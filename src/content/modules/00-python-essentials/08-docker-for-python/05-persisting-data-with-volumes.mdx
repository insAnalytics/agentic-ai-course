---
title: "Persisting data with volumes"
lesson: "08-docker-for-python"
order: 5
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import TerminalGroup from "../../../../components/lesson/TerminalGroup";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="The problem: a container's filesystem doesn't survive the container" variant="a">

[Recall from the images-vs-containers concept](/00-python-essentials/08-docker-for-python/02-images-vs-containers/#proving-the-isolation-live)
that each container has its own isolated filesystem, separate from the
image and from other containers. What wasn't shown there: that
filesystem is also **ephemeral** — removing a container removes
everything written inside it, including data you actually wanted to
keep.

Start a container running a small script that appends a line to
`/data/log.txt` each time it's called, call it a few times, then stop
and remove the container:

<TerminalGroup
  client:load
  panes={[
    {
      label: "Writing to a container's own filesystem",
      steps: [
        { command: "docker run --name logger my-app --once" },
        { command: "docker run --name logger2 my-app --once" },
        { command: "docker exec logger cat /data/log.txt" },
        { output: "entry from logger\nentry from logger2" },
        { command: "docker rm -f logger logger2" },
        { command: "docker run --rm my-app --read-log" },
        { output: "cat: /data/log.txt: No such file or directory" },
      ],
    },
  ]}
/>

A brand-new container, from the exact same image, has no memory of the
previous containers' writes — `/data/log.txt` never existed as far as
this new container is concerned, because it was never part of the
image, only ever written into now-deleted containers' own throwaway
filesystems.

</Subsection>

<Subsection title="Volumes: storage that outlives a container" variant="b">

A **volume** is storage that exists independently of any one container —
you attach it to a container with `-v`, and it survives that container
being stopped and removed, ready to be attached to a *different*
container later:

<TerminalGroup
  client:load
  panes={[
    {
      label: "Writing to a volume instead",
      steps: [
        { command: "docker volume create my-data" },
        { command: "docker run --name logger -v my-data:/data my-app --once" },
        { command: "docker rm -f logger" },
        { command: "docker run --rm -v my-data:/data my-app --read-log" },
        { output: "entry from logger" },
      ],
    },
  ]}
/>

`my-data:/data` means "mount the volume named `my-data` at `/data`
inside the container." The first container wrote to `/data/log.txt`
inside that volume; the second container — a completely fresh one, with
no relationship to the first beyond sharing the same volume — reads it
back successfully, because the volume itself was never deleted along
with the first container.

</Subsection>

<Subsection title="Named volumes vs. bind mounts" variant="a">

`docker volume create my-data` above makes a **named volume** — storage
Docker manages for you, in a location you don't need to know or care
about. The alternative is a **bind mount**: pointing directly at a real
path on your own machine instead:

```bash
docker run -v $(pwd)/local-data:/data my-app --once
```

Here, `/data` inside the container is literally `local-data` in your
current directory on the host — editable directly from outside the
container too, which is convenient for development (editing code live
without rebuilding) but less appropriate for something like a
database's actual data files, where a named volume, managed entirely by
Docker, is the more standard choice.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Why does data written inside a container disappear once that container is removed, even though the image it came from still exists?",
      options: [
        "This shouldn't happen — it's a bug if it does",
        "A container's filesystem is its own, separate from the image and other containers — removing the container removes that filesystem and everything written into it",
        "Images automatically absorb any changes made inside their containers",
        "Data is only lost if docker stop is used, not docker rm",
      ],
      correctIndex: 1,
      explanation:
        "A container's filesystem belongs to that specific container alone — nothing written into it is ever copied back into the image, so removing the container removes that data along with it.",
    },
    {
      question: "What does a Docker volume actually solve?",
      options: [
        "It makes a container start faster",
        "It provides storage that exists independently of any one container, so data survives that container being stopped and removed",
        "It automatically backs up an entire image",
        "It prevents a container from ever being removed",
      ],
      correctIndex: 1,
      explanation:
        "A volume lives outside any single container's own filesystem, so it isn't deleted when the container that was using it is stopped and removed — later containers can attach to the same volume and pick up right where the data was left.",
    },
    {
      question: "In docker run -v my-data:/data my-app, what does my-data:/data specify?",
      options: [
        "Two separate, unrelated environment variables",
        "Mount the volume named my-data at the path /data inside the container",
        "Rename the container to my-data",
        "Limit the container to /data amount of disk space",
      ],
      correctIndex: 1,
      explanation:
        "The syntax is volume-name:container-path — my-data is the volume being mounted, /data is where it appears inside the container's filesystem.",
    },
    {
      question: "What's the difference between a named volume and a bind mount?",
      options: [
        "They're identical in every way",
        "A named volume is storage Docker manages for you; a bind mount points directly at a real path on your own machine, editable from outside the container too",
        "Bind mounts can only be used for read-only data",
        "Named volumes require a running container to already exist first",
      ],
      correctIndex: 1,
      explanation:
        "A named volume's actual storage location is managed by Docker itself, out of sight; a bind mount is just a real directory on your own machine, mounted directly into the container, so changes are visible on both sides immediately.",
    },
  ]}
/>

</CheckpointZone>
