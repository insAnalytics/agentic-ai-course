---
title: "Why containers"
lesson: "08-docker-for-python"
order: 1
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import TerminalGroup from "../../../../components/lesson/TerminalGroup";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="The environment problem: what a venv can't reach" variant="a">

[A virtual environment, from the Python setup lesson](/00-python-essentials/01-python-basics/01-set-up-python/#virtual-environments-and-why-they-re-not-optional),
isolates *Python packages* — one project's `pip install`s don't leak into
another's. But a venv sits entirely *inside* one Python installation, on
one operating system — it has no ability to change or isolate anything
below that: not the Python version itself, not OS-level system
libraries, not the OS itself.

Both machines below start with an identical `requirements.txt` and an
identical venv setup script. Click Run on each pane and watch the same
commands play out differently depending on what's already on that
machine:

<TerminalGroup
  client:load
  panes={[
    {
      label: "Your machine — Ubuntu 22.04, libpq already installed",
      steps: [
        { command: "python3 -m venv .venv" },
        { command: "source .venv/bin/activate" },
        { command: "pip install -r requirements.txt" },
        { output: "Successfully installed psycopg2-2.9.9" },
        { command: "python3 app.py" },
        { output: "Connected to database. Server running on port 5000." },
      ],
    },
    {
      label: "A teammate's machine — Ubuntu 20.04, no libpq",
      steps: [
        { command: "python3 -m venv .venv" },
        { command: "source .venv/bin/activate" },
        { command: "pip install -r requirements.txt" },
        { output: "Successfully installed psycopg2-2.9.9" },
        { command: "python3 app.py" },
        {
          output:
            "ImportError: libpq.so.5: cannot open shared object file: No such file or directory",
        },
      ],
    },
  ]}
/>

Same `requirements.txt`, same venv commands — it still fails, because the
missing piece is a *system* library, something a venv was never able to
touch in the first place. Now both panes run `docker build -t my-app .`
and `docker run my-app` instead — identical output on both, since neither
machine's own OS is what's actually running the code anymore:

<TerminalGroup
  client:load
  panes={[
    {
      label: "Your machine",
      steps: [
        { command: "docker build -t my-app ." },
        { output: "Successfully built 9w0x1y2z3a4b" },
        { output: "Successfully tagged my-app:latest" },
        { command: "docker run my-app" },
        { output: "Connected to database. Server running on port 5000." },
      ],
    },
    {
      label: "A teammate's machine",
      steps: [
        { command: "docker build -t my-app ." },
        { output: "Successfully built 9w0x1y2z3a4b" },
        { output: "Successfully tagged my-app:latest" },
        { command: "docker run my-app" },
        { output: "Connected to database. Server running on port 5000." },
      ],
    },
  ]}
/>

</Subsection>

<Subsection title="A second, different reason: restricting what code can touch" variant="b">

There's a motivation for containers that matters specifically for the
kind of code this course is building toward, separate from "it runs the
same everywhere": **isolation as a security boundary**, not just a
consistency guarantee. An agent that executes tool calls, or runs code an
LLM generated, is running code whose exact behavior you don't fully
control in advance — restricting what that code can actually *reach* (the
filesystem, the network, environment variables holding credentials)
matters in a way it doesn't for code you wrote yourself and trust
completely.

A small script standing in for "an agent's generated code" — it lists
the contents of `/`, reads an environment variable named
`AWS_SECRET_KEY`, and attempts to make an outbound network request.
Click Run on each pane to compare running it directly on the host versus
inside a minimal, locked-down container:

<TerminalGroup
  client:load
  panes={[
    {
      label: "Run directly on the host",
      steps: [
        { command: "python3 agent_generated_code.py" },
        { output: "/: bin  boot  dev  etc  home  lib  proc  root  srv  usr  var" },
        { output: "AWS_SECRET_KEY: sk-live-a1b2c3d4e5f6..." },
        { output: "outbound request to evil.example.com: succeeded" },
      ],
    },
    {
      label: "Run inside a minimal sandboxed container",
      steps: [
        { command: "docker run --rm --network none -v $(pwd)/project:/app my-agent-sandbox" },
        { output: "/: app" },
        { output: "AWS_SECRET_KEY: (not set)" },
        { output: "outbound request to evil.example.com: failed — network is unreachable" },
      ],
    },
  ]}
/>

Same code, dramatically different capabilities — not because the code
changed, but because the boundary around it did. This is the shape of
the actual problem: you don't need to fully trust code to safely run it,
if the container around it only exposes exactly what that code is
supposed to need.

</Subsection>

<Subsection title="A trap the isolation motivation makes worse, not better: secrets in images" variant="a">

Isolation only helps if you don't undermine it yourself — and there's a
specific, easy mistake worth naming here: baking a secret (an API key, a
`.env` file, a database password) directly into an image, rather than
passing it in at `docker run` time (the way `-e` does, covered later in
this lesson's concept on building and running a container). A
`COPY .env .` instruction, or a hardcoded key in a `RUN` command, means
that secret is now baked permanently into the image itself — present in
every layer, every container built from it, and anywhere that image
ever gets pushed or shared, even if the file is later deleted in a
subsequent instruction. Deleting a file in a later layer doesn't remove
it from the earlier layer where it was actually added — the same
layer-caching mechanics covered later in this lesson mean the earlier
layer, secret included, is still sitting there in the image's history.
The fix, previewed here and covered properly later: pass secrets in at
run time, never bake them into the build.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "What does a virtual environment's isolation boundary actually cover?",
      options: [
        "The entire operating system, including system libraries",
        "Only Python packages within one Python installation — it can't isolate or change the OS or system libraries beneath it",
        "Only the specific Python version being used",
        "Nothing — venvs don't actually isolate anything",
      ],
      correctIndex: 1,
      explanation:
        "A venv's isolation stops at the Python interpreter — the OS, system libraries, and even which Python version is available to build the venv from are all shared with, and dependent on, the underlying machine.",
    },
    {
      question: "Beyond making code run consistently across machines, what's the second motivation for containers this section introduces?",
      options: [
        "Containers make code run faster",
        "Restricting what running code can actually access — filesystem, network, credentials — as a genuine security boundary, not just a consistency guarantee",
        "Containers eliminate the need for testing",
        "Containers automatically fix bugs in the code they run",
      ],
      correctIndex: 1,
      explanation:
        "This is a genuinely different motivation than consistency — restricting what code can reach matters specifically because the code's own behavior isn't something you can fully vouch for in advance, which describes agent-generated or tool-executed code exactly.",
    },
    {
      question:
        "In the isolation demo, why does the containerized version fail to read AWS_SECRET_KEY or reach evil.example.com, when the exact same script succeeds at both when run directly on the host?",
      options: [
        "The script itself is different in the container",
        "The container simply doesn't have that environment variable passed in, and its network access is explicitly disabled — the code's capabilities are limited by what the container exposes, not by the code itself",
        "Containers can't make network requests at all, ever",
        "AWS_SECRET_KEY is a reserved name Docker always blocks",
      ],
      correctIndex: 1,
      explanation:
        "Nothing about the script changed between the two runs — only what the container actually exposed to it did. Removing the environment variable and disabling networking means the code's capabilities are limited by the boundary around it, not by anything in the code itself.",
    },
    {
      question:
        "Why does deleting a secret file in a later Dockerfile instruction (e.g., RUN rm .env) fail to actually remove it from the resulting image?",
      options: [
        "rm doesn't work inside Docker builds",
        "The file was already baked into an earlier layer when it was copied in — a later layer deleting it doesn't erase it from that earlier layer, which is still part of the image",
        "This actually does fully remove it — there's no real risk here",
        "Only COPY instructions can be deleted, not RUN instructions",
      ],
      correctIndex: 1,
      explanation:
        "Each Dockerfile instruction bakes a new, immutable layer — a later RUN rm doesn't edit the earlier layer, it just adds a new layer on top that hides the file at runtime. The secret is still physically present in the image's layer history, retrievable by anyone who has the image.",
    },
  ]}
/>

</CheckpointZone>
