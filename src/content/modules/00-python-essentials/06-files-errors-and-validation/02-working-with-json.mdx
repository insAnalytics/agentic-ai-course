---
title: "Working with JSON"
lesson: "06-files-errors-and-validation"
order: 2
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import LiveDemo from "../../../../components/lesson/LiveDemo";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="JSON is just text — and it maps directly onto dicts and lists" variant="a">

JSON (JavaScript Object Notation) is a plain-text format for structured
data — and not coincidentally, it's shaped almost exactly like Python's
own dicts and lists, which is a large part of why it's the default format
for API requests/responses, config files, and tool-call arguments alike:

```json
{
    "name": "research_agent",
    "model": "claude-sonnet",
    "temperature": 0.7,
    "tags": ["search", "reasoning"]
}
```
*(a plain JSON file, shown for reference — not Python code, not run live)*

Read that side by side with a Python dict literal and the resemblance is
immediate:

- JSON objects become dicts
- JSON arrays become lists
- JSON strings/numbers/booleans/`null` become `str`/`int` or `float`/
  `bool`/`None`

The `json` module (part of the standard library — no install needed)
converts between the two directly.

</Subsection>

<Subsection title="Reading JSON from a file: json.load" variant="b">

<LiveDemo
  client:load
  setupCode={`import json

with open("config.json", "w") as f:
    json.dump({"name": "research_agent", "model": "claude-sonnet", "temperature": 0.7, "tags": ["search", "reasoning"]}, f)`}
  code={`import json

with open("config.json", "r") as f:
    config = json.load(f)

print(config)
print(type(config))
print(config["temperature"])`}
/>

`json.load(f)` takes an already-open file object (note: not a filename —
you still need
[`with open(...)` from the previous section](/00-python-essentials/06-files-errors-and-validation/01-file-io-and-context-managers/#with-guaranteed-cleanup-even-on-exception)
to get one) and parses its entire contents into ordinary Python data —
here, a `dict`, since the file's top level is a JSON object. Once parsed,
it's just a regular dict — every
[dict operation from the data structures lesson](/00-python-essentials/02-collections/03-dicts/)
applies normally.

</Subsection>

<Subsection title="Writing JSON to a file: json.dump" variant="a">

The reverse direction — converting Python data into JSON text and writing
it to a file — uses `json.dump`:

```python
import json

config = {
    "name": "research_agent",
    "model": "claude-sonnet",
    "temperature": 0.7,
    "tags": ["search", "reasoning"],
}

with open("config.json", "w") as f:
    json.dump(config, f, indent=2)
```
*(writes a file — not run live in this format, but produces the exact
file shown at the top of this section, reformatted with indent=2)*

`indent=2` is optional but worth using by default for anything a human
might read later — without it, `json.dump` writes the most compact form
possible, all on one line, which is valid JSON but painful to read.

</Subsection>

<Subsection title="json.loads / json.dumps — the string versions" variant="b">

Sometimes JSON arrives as a string you already have in memory — an API
response body, for instance — rather than something sitting in a file.
`json.loads` (load **s**tring) and `json.dumps` (dump to a **s**tring) do
the same conversions, without any file involved:

<LiveDemo client:load code={`import json

json_text = '{"tool": "search", "arguments": {"query": "weather"}}'
parsed = json.loads(json_text)

print(parsed)
print(parsed["arguments"]["query"])

back_to_text = json.dumps(parsed)
print(back_to_text)`} />

The naming is consistent across all four: `load`/`dump` work with an
already-open file object; `loads`/`dumps` work with a plain string — the
`s` suffix specifically flags "string," not "load safely" or anything
else. It's easy to reach for the wrong one out of habit, so it's worth
checking which you actually have (a file object, or a string) before
picking which function to call.

</Subsection>

<Subsection title="When the JSON itself is malformed: JSONDecodeError" variant="a">

Parsing can fail — the text might not actually be valid JSON at all
(a trailing comma, a missing quote, truncated data from a network error):

<LiveDemo client:load code={`import json

broken_json = '{"name": "research_agent", "model": }'
config = json.loads(broken_json)`} />

`JSONDecodeError` is a normal exception, catchable exactly like any other
[from the Python setup lesson's error handling concept](/00-python-essentials/01-python-basics/03-error-handling/#the-basic-shape-and-how-it-compares):

<LiveDemo client:load code={`import json

broken_json = '{"name": "research_agent", "model": }'

try:
    config = json.loads(broken_json)
except json.JSONDecodeError:
    print("couldn't parse that as JSON")
    config = {}

print(config)`} />

This matters specifically because JSON so often arrives from somewhere
you don't control — a file someone else edited by hand, a flaky network
response — so treating parsing as something that *can* fail, rather than
assuming it always succeeds, is the realistic default here.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "What Python type does a top-level JSON object ({...}) become once parsed?",
      options: ["A list", "A dict", "A string", "A tuple"],
      correctIndex: 1,
      explanation: "JSON objects map directly onto Python dicts — key-value pairs, looked up by key.",
    },
    {
      question: "What's the difference between json.load and json.loads?",
      options: [
        "No difference, they're aliases",
        "json.load reads from an already-open file object; json.loads parses an in-memory string",
        "json.loads is only for lists, not dicts",
        "json.load is deprecated in favor of json.loads",
      ],
      correctIndex: 1,
      explanation:
        'The s suffix specifically means "string" — load/dump expect a file object, loads/dumps expect/produce a plain string.',
    },
    {
      question:
        'Why call with open("config.json", "r") as f: before json.load(f), rather than passing the filename directly to json.load?',
      options: [
        "json.load doesn't accept file objects at all",
        "json.load expects an already-open file object, not a filename — you still need with open(...) to produce one",
        "json.load only works with .txt files",
        "There's no real reason, either would work identically",
      ],
      correctIndex: 1,
      explanation:
        "json.load parses from a file object's contents — getting that file object still requires opening the file yourself first.",
    },
    {
      question: "What does indent=2 in json.dump(config, f, indent=2) actually change?",
      options: [
        "It changes which keys get included in the output",
        "It formats the written JSON with 2-space indentation for readability — without it, the output is a single compact line",
        "It limits the file to 2 levels of nesting",
        "It's required for json.dump to work at all",
      ],
      correctIndex: 1,
      explanation:
        "indent is purely a formatting option — both forms produce valid, equivalent JSON, just easier or harder for a human to read.",
    },
    {
      question: "What does JSONDecodeError represent, and when does it occur?",
      options: [
        "A network connectivity problem",
        "The text being parsed isn't actually valid JSON — a syntax problem in the JSON text itself, raised by json.load/json.loads",
        "A missing file",
        "A Python version incompatibility",
      ],
      correctIndex: 1,
      explanation:
        "JSONDecodeError signals a syntax problem in the JSON text itself — a trailing comma, missing value, or otherwise malformed structure — not a networking or file-system issue; catching it is the standard way to handle JSON that might arrive malformed.",
    },
  ]}
/>

</CheckpointZone>
