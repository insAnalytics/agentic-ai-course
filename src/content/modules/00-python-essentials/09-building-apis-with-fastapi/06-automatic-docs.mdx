---
title: "Automatic docs: /docs and /redoc"
lesson: "09-building-apis-with-fastapi"
order: 6
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import TerminalGroup from "../../../../components/lesson/TerminalGroup";
import ApiDocsPreview from "../../../../components/lesson/ApiDocsPreview.astro";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="Documentation you didn't write" variant="a">

Every FastAPI app automatically serves two documentation pages,
generated entirely from what you've already written — no separate
documentation effort, no extra annotations beyond what's already in
your route signatures and Pydantic models:

- **`/docs`** — an interactive UI (Swagger UI) listing every route,
  grouped and expandable, with a "Try it out" button that sends a real
  request from the browser and shows the actual response.
- **`/redoc`** — a cleaner, read-only reference view (ReDoc) of the
  same information, better suited to sharing as reference
  documentation than to interactively poking at.

Both are generated from the exact same source: an **OpenAPI schema**
FastAPI builds automatically by inspecting your routes, their
parameters, and their Pydantic models — visiting `/openapi.json` on
any FastAPI app shows that schema directly, as raw JSON.

</Subsection>

<Subsection title="What ends up in the docs, and where it comes from" variant="b">

Nothing here is new information — it's everything from this lesson so
far, surfaced automatically:

```python
from fastapi import FastAPI, Query
from pydantic import BaseModel, Field

app = FastAPI()

class AgentConfig(BaseModel):
    name: str = Field(description="the agent's unique identifier")
    model: str = Field(description="which LLM this agent runs on")
    temperature: float = Field(default=0.7, description="sampling temperature, 0 to 1")

@app.post("/agents", response_model=AgentConfig, status_code=201)
def create_agent(config: AgentConfig):
    """Register a new agent configuration."""
    return config

@app.get("/agents")
def list_agents(limit: int = Query(default=10, gt=0, le=100, description="max results to return")):
    """List existing agents, most recently created first."""
    return []
```

This is what visiting `/docs` for that app actually looks like —
interactive, with a (non-functional here) "Try it out" per route:

<ApiDocsPreview
  variant="swagger"
  routes={[
    {
      method: "POST",
      path: "/agents",
      summary: "Create Agent",
      description: "Register a new agent configuration.",
      requestBody: [
        { name: "name", type: "string", description: "the agent's unique identifier", required: true },
        { name: "model", type: "string", description: "which LLM this agent runs on", required: true },
        { name: "temperature", type: "number", description: "sampling temperature, 0 to 1 (default 0.7)" },
      ],
    },
    {
      method: "GET",
      path: "/agents",
      summary: "List Agents",
      description: "List existing agents, most recently created first.",
      parameters: [{ name: "limit", type: "integer", description: "max results to return (default 10, 1-100)" }],
    },
  ]}
/>

`/redoc` shows the same underlying schema, laid out as a cleaner
reference page instead of an interactive one:

<ApiDocsPreview
  variant="redoc"
  routes={[
    {
      method: "POST",
      path: "/agents",
      summary: "Create Agent",
      description: "Register a new agent configuration.",
      requestBody: [
        { name: "name", type: "string", description: "the agent's unique identifier", required: true },
        { name: "model", type: "string", description: "which LLM this agent runs on", required: true },
        { name: "temperature", type: "number", description: "sampling temperature, 0 to 1 (default 0.7)" },
      ],
    },
    {
      method: "GET",
      path: "/agents",
      summary: "List Agents",
      description: "List existing agents, most recently created first.",
      parameters: [{ name: "limit", type: "integer", description: "max results to return (default 10, 1-100)" }],
    },
  ]}
/>

And underneath both of those is the same raw OpenAPI schema:

<TerminalGroup
  client:load
  panes={[
    {
      label: "The schema behind the docs",
      steps: [
        { command: "curl http://127.0.0.1:8000/openapi.json" },
        { output: "{" },
        { output: '  "paths": {' },
        { output: '    "/agents": {' },
        { output: '      "post": {' },
        { output: '        "summary": "Create Agent",' },
        { output: '        "description": "Register a new agent configuration."' },
        { output: "      }," },
        { output: '      "get": {' },
        { output: '        "summary": "List Agents",' },
        { output: '        "description": "List existing agents, most recently created first."' },
        { output: "      }" },
        { output: "    }" },
        { output: "  }," },
        { output: '  "components": {' },
        { output: '    "schemas": {' },
        { output: '      "AgentConfig": {' },
        { output: '        "properties": {' },
        { output: '          "temperature": {' },
        { output: '            "type": "number",' },
        { output: '            "description": "sampling temperature, 0 to 1",' },
        { output: '            "default": 0.7' },
        { output: "          }" },
        { output: "        }" },
        { output: "      }" },
        { output: "    }" },
        { output: "  }" },
        { output: "}" },
      ],
    },
  ]}
/>

Visiting `/docs` for this app shows: both routes, each with its
docstring —
[exactly the same docstring mechanism from the functions lesson](/00-python-essentials/03-functions-and-reuse/01-function-fundamentals/#docstrings),
now read by FastAPI's docs generator instead of an LLM tool-calling
framework, though it's worth noticing these are genuinely the same
underlying idea — as its description; `create_agent`'s expected
request body shown field-by-field, each with its
`Field(description=...)` text and whether it's required;
`list_agents`'s `limit` parameter shown as optional, with its range
constraint and description visible directly, exactly as the schema
excerpt above shows.

</Subsection>

<Subsection title='Why this matters more than "nice to have"' variant="a">

This isn't just convenient for humans browsing the API — it's the same
throughline as
[why type hints and docstrings mattered for tool-calling frameworks back in the functions lesson](/00-python-essentials/03-functions-and-reuse/01-function-fundamentals/#type-hints):
a machine-readable OpenAPI schema, generated the same automatic way,
is exactly the kind of structured description an LLM-based
tool-calling system can consume directly to know what an endpoint
expects and returns — the same underlying description serving both a
human reading `/docs` and, potentially, an agent deciding how to call
your API as a tool.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "What generates the content shown at /docs and /redoc?",
      options: [
        "A separate documentation file the developer must write by hand",
        "An OpenAPI schema FastAPI builds automatically by inspecting your routes, parameters, and Pydantic models",
        "Comments manually added to each route",
        "A third-party service FastAPI calls out to",
      ],
      correctIndex: 1,
      explanation:
        "Both docs pages read from the same automatically-generated OpenAPI schema — nothing is hand-written beyond the route signatures and models that already exist for other reasons.",
    },
    {
      question: "What's the practical difference between /docs and /redoc?",
      options: [
        "They show completely unrelated information",
        "Both are generated from the same underlying schema; /docs is interactive (Swagger UI, with \"Try it out\"), /redoc is a cleaner read-only reference view",
        "/redoc only works for GET routes",
        "/docs requires a separate server to run",
      ],
      correctIndex: 1,
      explanation:
        "Same source schema, two different presentations — one built for interactively trying requests, the other for reading as reference documentation.",
    },
    {
      question: "Where does a route's description shown in /docs come from?",
      options: [
        "It must be added with a special @app.description(...) decorator",
        "The route function's own docstring — the same mechanism from the functions lesson, now read by FastAPI's docs generator",
        "FastAPI generates a generic description automatically, ignoring docstrings",
        "Descriptions can only be added to Pydantic model fields, not routes",
      ],
      correctIndex: 1,
      explanation:
        "A route's docstring is exactly what shows up as its description in the docs — no separate annotation needed, since FastAPI reads the docstring that's already there.",
    },
    {
      question: 'What does Field(description="...") on a Pydantic model field add to the automatic docs?',
      options: [
        "Nothing — Field() only affects validation, never documentation",
        "A human-readable description of that specific field, shown alongside its type and whether it's required in the generated docs",
        "It changes the field's actual validation rules",
        "It's required for the field to appear in /docs at all",
      ],
      correctIndex: 1,
      explanation:
        "Field(description=...) feeds directly into the generated schema — it shows up per-field in the docs, alongside that field's type and required/optional status.",
    },
    {
      question: "Why does this section connect FastAPI's automatic docs back to the functions lesson's point about type hints and docstrings mattering for tool-calling frameworks?",
      options: [
        "It doesn't — the two are unrelated",
        "Both are the same underlying idea: a structured, machine-readable description generated from type hints and docstrings, usable by a human reading documentation or a system (like an LLM) deciding how to call something",
        "FastAPI's docs only work if the app is also used as an LLM tool",
        "Docstrings are required for a FastAPI app to run at all",
      ],
      correctIndex: 1,
      explanation:
        "The same type hints and docstrings that make a function callable as an LLM tool are what generate FastAPI's docs here — one mechanism, serving both a human reader and a machine consumer.",
    },
  ]}
/>

</CheckpointZone>
