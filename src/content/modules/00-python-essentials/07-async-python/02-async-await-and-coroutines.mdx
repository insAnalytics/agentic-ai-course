---
title: "async/await and coroutines"
lesson: "07-async-python"
order: 2
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import LiveDemo from "../../../../components/lesson/LiveDemo";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="The syntax, compared to what you know" variant="a">

If you've used JavaScript, this will look immediately familiar — Python's
`async`/`await` is deliberately similar in spelling and purpose. Go
(goroutines) and Java (`CompletableFuture`) solve the same underlying
problem — running many I/O-bound operations without blocking each other —
with different syntax and a different underlying model; Python's is
closest to JS's.

<LiveDemo client:load code={`import asyncio

async def call_search_api():
    print("calling search API...")
    await asyncio.sleep(2)
    return "3 results found"

asyncio.run(call_search_api())`} />

`async def` marks a function as a **coroutine function** — a function
that can be paused and resumed, rather than running start-to-finish in
one uninterrupted block the way every function has so far. `await`
inside it marks a specific point where it's willing to pause: "wait here
for `asyncio.sleep(2)` to finish, and let something else run in the
meantime, before continuing past this line."

Note the return value wasn't printed above — `asyncio.run(...)` runs the
coroutine but this example didn't do anything with what it returned.
`asyncio.run()` is the entry point: the thing that actually starts
Python's event loop and runs a coroutine to completion. You'll almost
always see exactly one `asyncio.run()` call, at the very top level of a
program — everything else happens through `await`, inside other
coroutines.

</Subsection>

<Subsection title="The gotcha: calling a coroutine function doesn't run it" variant="b">

This is worth seeing directly, since it's a genuinely common mistake:
calling an `async def` function the normal way — without `await` — does
*not* execute its body. It returns a **coroutine object** instead, a
paused, not-yet-started task description:

<LiveDemo client:load code={`import asyncio

async def call_search_api():
    print("calling search API...")
    await asyncio.sleep(2)
    return "3 results found"

result = call_search_api()   # looks like a normal call...
print(result)
print(type(result))`} />

Notice `"calling search API..."` never printed — the function body never
actually ran. `call_search_api()` on its own just *creates* a coroutine
object; it doesn't start it. Getting it to actually run requires either
`await`-ing it (from inside another coroutine) or handing it to
`asyncio.run()` (from regular, non-async code) — one of the two has to
actually drive it forward:

<LiveDemo client:load code={`import asyncio

async def call_search_api():
    print("calling search API...")
    await asyncio.sleep(2)
    return "3 results found"

result = asyncio.run(call_search_api())
print(result)`} />

If you ever see a warning in the wild like `RuntimeWarning: coroutine
'...' was never awaited`, this is exactly what happened — a coroutine
object got created and then simply discarded, its body never actually
executing.

</Subsection>

<Subsection title="await-ing from inside another coroutine" variant="a">

The more common shape than a single top-level `asyncio.run()` call:
one coroutine `await`s another, chaining them together, with only the
outermost one actually passed to `asyncio.run()`:

<LiveDemo client:load code={`import asyncio

async def call_search_api():
    print("calling search API...")
    await asyncio.sleep(2)
    return "3 results found"

async def main():
    result = await call_search_api()
    print(f"got: {result}")

asyncio.run(main())`} />

`main()` itself is a coroutine function too — `async def` all the way
through. `await call_search_api()` inside it is what actually runs
`call_search_api`'s body and waits for its result, exactly the same
mechanism as before, just one level deeper. This nested-`await` shape —
one `async def` function calling `await` on another — is how real
`async` code is actually structured; a single, flat `asyncio.run()` call
on one coroutine, like the earlier examples, is really just the smallest
possible version of that same pattern.

Nothing here is concurrent yet, though — `await call_search_api()` still
waits for it to fully finish before `main()` moves on to its next line,
same as a regular function call would. Making multiple `await`-able
operations actually overlap is exactly what the next section,
`asyncio.gather()`, is for.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "What does async def mark a function as?",
      options: [
        "A function that runs faster than a normal function",
        "A coroutine function — one that can be paused and resumed, rather than running start-to-finish uninterrupted",
        "A function that can never raise an exception",
        "A function that runs on a separate CPU core",
      ],
      correctIndex: 1,
      explanation:
        "async def doesn't change execution speed — it changes the function's control-flow shape, giving it explicit pause points (await) instead of the usual uninterrupted start-to-finish run.",
    },
    {
      question: "What actually happens when you call an async def function without await, like result = call_search_api()?",
      options: [
        "The function body runs normally and result holds its return value",
        "It returns a coroutine object without running the function's body at all — nothing inside it executes yet",
        "It raises a SyntaxError",
        "It runs the function body in a separate thread automatically",
      ],
      correctIndex: 1,
      explanation:
        "Calling an async def function only builds a coroutine object describing what would run — nothing inside the function body executes until something actually drives that coroutine forward with await or asyncio.run().",
    },
    {
      question: "What is asyncio.run() for?",
      options: [
        "It's optional — coroutines run automatically without it",
        "It's the entry point that actually starts the event loop and drives a coroutine to completion — typically called exactly once, at the top level of a program",
        "It only works inside another coroutine",
        "It converts a coroutine function into a regular function",
      ],
      correctIndex: 1,
      explanation:
        "asyncio.run() is what actually starts the event loop and pushes a coroutine through to completion — without it (or an enclosing await chain driven by it), a coroutine object just sits there, created but never advanced.",
    },
    {
      question: "A RuntimeWarning: coroutine '...' was never awaited message most directly indicates what?",
      options: [
        "A network timeout occurred",
        "An async def function was called without await or asyncio.run(), so a coroutine object was created and then discarded without ever running",
        "The event loop crashed",
        "asyncio isn't installed correctly",
      ],
      correctIndex: 1,
      explanation:
        "This warning is Python's way of flagging exactly the mistake from the previous section — a coroutine was created by calling an async def function, but nothing ever awaited or ran it, so its body silently never executed.",
    },
    {
      question: "In async def main(): result = await call_search_api(), does main() continue past that line before call_search_api() finishes?",
      options: [
        "Yes — await starts it and moves on immediately",
        "No — await still waits for call_search_api() to fully finish before continuing, the same as a regular function call, just written differently",
        "It depends on whether asyncio.run() was used",
        "main() runs call_search_api() twice",
      ],
      correctIndex: 1,
      explanation:
        "await pauses the calling coroutine at that exact line until the awaited call finishes — it's not fire-and-forget; execution only resumes past it once the result is actually ready, the same sequencing as a normal call.",
    },
  ]}
/>

</CheckpointZone>
