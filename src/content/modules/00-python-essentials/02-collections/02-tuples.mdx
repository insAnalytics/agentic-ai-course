---
title: "Tuples"
lesson: "02-collections"
order: 2
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import LiveDemo from "../../../../components/lesson/LiveDemo";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import GradedExercise from "../../../../components/lesson/GradedExercise";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="Literal syntax, and how it differs from a list" variant="a">

A tuple looks almost like a list, but uses parentheses instead of brackets
— and, crucially, it's immutable:

<LiveDemo client:load code={`point = (3, 5)
print(point[0])   # indexing works the same as lists`} />

<LiveDemo client:load code={`point = (3, 5)
point[0] = 10`} />

This is the whole point of a tuple: once created, it can't be changed. If
you're coming from Java, there's no exact built-in equivalent pre-records
(Java 16+); it's closer to a fixed, unmodifiable array. C#'s tuple type is a
much closer match syntactically.

</Subsection>

<Subsection title="Why use a tuple instead of a list" variant="b">

Immutability is a signal, not just a restriction — a tuple says "this is a
fixed, small collection of related values that won't change," while a list
says "this is a growable collection." A coordinate pair, an RGB color, a
(name, age) record — these are naturally tuples, because there's no reason
to append a fourth value to a coordinate.

```python
rgb = (255, 0, 0)     # a color has exactly 3 parts, always
tools = ["calculator", "search"]   # a list of tools can grow
```

</Subsection>

<Subsection title="Unpacking" variant="a">

You can assign a tuple's values directly to multiple variables in one line:

<LiveDemo client:load code={`point = (3, 5)
x, y = point

print(x)
print(y)`} />

This also explains something you may not have thought twice about: when a
Python function "returns multiple values" with `return a, b, c`, it's
actually returning a single tuple — the comma-separated values on the right
of `return` get packed into one automatically, and `a, b, c = some_function()`
unpacks them back out on the receiving end.

<LiveDemo client:load code={`def min_max(numbers):
    return min(numbers), max(numbers)

lowest, highest = min_max([4, 1, 9, 2])
print(lowest, highest)`} />

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "What happens when you try to run point[0] = 10 on a tuple?",
      options: [
        "It works, tuples are just lists with different brackets",
        "TypeError — tuples don't support item assignment",
        "It silently does nothing",
        "IndexError",
      ],
      correctIndex: 1,
      explanation: "Tuples are immutable — once created, their contents can't be reassigned.",
    },
    {
      question: "When would a tuple be the better choice over a list?",
      options: [
        "When you need to add and remove items frequently",
        "When the collection has a fixed, known size and shouldn't change, like a coordinate pair",
        "Tuples are always better than lists",
        "When you need to sort the values",
      ],
      correctIndex: 1,
      explanation:
        "A tuple communicates \"fixed structure, won't grow or shrink\" — a list communicates the opposite.",
    },
    {
      question: "What's actually happening when a function does return a, b, c?",
      options: [
        "Python returns three separate values simultaneously, unrelated to tuples",
        "The three values are packed into a single tuple, which is what's actually returned",
        "This is invalid syntax outside of a list",
        "Only the last value (c) is returned",
      ],
      correctIndex: 1,
      explanation:
        "return a, b, c packs the values into one tuple object. x, y, z = some_function() then unpacks that tuple back into separate variables on the receiving end.",
    },
  ]}
/>

</CheckpointZone>

<CheckpointZone>

<GradedExercise
  client:load
  task="For each point, unpack it into x, y, and calculate its distance from target_x as abs(x - target_x). Keep track of the point with the smallest distance seen so far as you loop through the list, and return it at the end. (Composes tuple unpacking with a running comparison, no sorting needed.)"
  starterCode={`def closest_pair(points, target_x):
    """
    points: a list of (x, y) tuples.
    target_x: a number.
    Return the single (x, y) tuple whose x-value is closest to target_x.
    E.g. closest_pair([(1, 5), (8, 2), (4, 9)], 5) -> (4, 9)
    """
    # TODO: implement using tuple unpacking and a running comparison
    pass`}
  hiddenTests={[
    "assert closest_pair([(1, 5), (8, 2), (4, 9)], 5) == (4, 9)",
    "assert closest_pair([(0, 0), (10, 0)], 9) == (10, 0)",
    "assert closest_pair([(3, 1)], 100) == (3, 1)",
  ]}
  hint="Start by assuming the first point is closest, and track its distance. Loop through the rest, unpack each into x, y, compute abs(x - target_x), and update your 'closest so far' whenever you find a smaller distance."
  correctAnswer={{
    code: `def closest_pair(points, target_x):
    closest = points[0]
    smallest_distance = abs(closest[0] - target_x)

    for point in points:
        x, y = point
        distance = abs(x - target_x)
        if distance < smallest_distance:
            smallest_distance = distance
            closest = point

    return closest`,
    explanation:
      "This loops through the points, unpacking each (x, y) tuple to check its distance from target_x, and keeps track of the closest one seen so far — combining tuple unpacking with the loop/comparison pattern from earlier in the module.",
  }}
/>

</CheckpointZone>
