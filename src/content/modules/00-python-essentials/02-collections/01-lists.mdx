---
title: "Lists"
lesson: "02-collections"
order: 1
---
import Subsection from "../../../../components/lesson/Subsection.astro";
import LiveDemo from "../../../../components/lesson/LiveDemo";
import QuizGroup from "../../../../components/lesson/QuizGroup";
import GradedExercise from "../../../../components/lesson/GradedExercise";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<Subsection title="If you're coming from another language" variant="a">

Python's `list` is closer to Java's `ArrayList` or JavaScript's `Array` than
to a C-style fixed array — it resizes automatically and isn't restricted to
one type:

<LiveDemo client:load code={`mixed = [1, "two", 3.0, True]   # perfectly valid — no single type required
print(mixed)`} />

This flexibility is convenient, but it also means Python won't catch a type
mistake in a list at compile time the way a strictly-typed array would —
worth keeping in mind given what we covered about
[runtime vs. compile-time errors](/00-python-essentials/01-python-basics/01-set-up-python/#execution-model-compared-to-what-you-already-know).

</Subsection>

<Subsection title="Literal syntax and indexing" variant="b">

A list is an ordered, mutable collection — you've already seen
[the syntax used without explanation earlier](/00-python-essentials/01-python-basics/02-control-flow-and-syntax/#for-loops-iterate-over-values-not-a-counter):

<LiveDemo client:load code={`tools = ["calculator", "search", "memory"]

print(tools[0])      # first item
print(tools[-1])      # last item — negative indices count from the end`} />

Negative indexing is Python-specific — if you're coming from Java/C++,
there's no `tools[tools.length - 1]` needed; `-1` just means "last."

</Subsection>

<Subsection title="Slicing" variant="a">

`list[start:stop]` pulls a sub-list — `start` is inclusive, `stop` is
exclusive:

<LiveDemo client:load code={`numbers = [10, 20, 30, 40, 50]

print(numbers[1:3])     # index 1 up to (not including) 3
print(numbers[:2])      # from the start
print(numbers[2:])      # to the end
print(numbers[-2:])     # last two, using negative indices`} />

</Subsection>

<Subsection title="Mutability and common methods" variant="b">

Lists can be changed in place after creation — this is different from
strings, which are immutable in Python (an operation like `.upper()`
returns a *new* string rather than modifying the original):

<LiveDemo client:load code={`tools = ["calculator", "search"]

tools.append("memory")           # add to the end
tools.insert(0, "planner")       # insert at a specific index
tools.remove("search")           # remove by value
last = tools.pop()                # remove and return the last item

print(tools)
print(last)`} />

<LiveDemo client:load code={`numbers = [3, 1, 4, 1, 5]
numbers.sort()
print(numbers)`} />

`.sort()` modifies the list in place and returns `None`; it doesn't return
the sorted list. If you want a sorted copy without touching the original,
use `sorted(numbers)` instead, which returns a new list.

</Subsection>

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Which existing data structure is Python's list closest to?",
      options: [
        "A fixed-size C-style array",
        "Java's ArrayList or JS's Array — resizable, no single required type",
        "A Java primitive array",
        "There's no reasonable comparison",
      ],
      correctIndex: 1,
      explanation:
        "A Python list resizes automatically and can hold mixed types, unlike a fixed, single-type C-style array.",
    },
    {
      question: "What does tools[-1] return?",
      options: [
        "An error — negative indices aren't valid",
        "The last item in the list",
        "The first item, counted backwards",
        "None",
      ],
      correctIndex: 1,
      explanation:
        "Negative indices count from the end — -1 is the last item, -2 the second-to-last, and so on.",
    },
    {
      question: "What does numbers[1:3] include, given numbers = [10, 20, 30, 40, 50]?",
      options: ["[20, 30]", "[20, 30, 40]", "[10, 20, 30]", "[30]"],
      correctIndex: 0,
      explanation: "Slicing is inclusive of start and exclusive of stop — index 1 and 2, not 3.",
    },
    {
      question: "After numbers.sort(), what does the expression itself return?",
      options: [
        "The sorted list",
        "None — .sort() mutates in place and returns nothing",
        "A boolean indicating success",
        "The number of items sorted",
      ],
      correctIndex: 1,
      explanation:
        "Methods that mutate a list in place (like .sort(), .append()) typically return None. Use sorted(numbers) if you want a new sorted list back as a value.",
    },
  ]}
/>

</CheckpointZone>

<CheckpointZone>

<GradedExercise
  client:load
  task="Sort the scores in descending order, then slice out the top two."
  starterCode={`def top_two_scores(scores):
    """
    scores: a list of integers.
    Return the two highest scores, as a list, in descending order.
    E.g. [5, 1, 9, 3] -> [9, 5]
    """
    # TODO: implement using sorting and slicing
    pass`}
  hiddenTests={[
    "assert top_two_scores([5, 1, 9, 3]) == [9, 5]",
    "assert top_two_scores([1, 2]) == [2, 1]",
    "assert top_two_scores([7, 7, 7]) == [7, 7]",
  ]}
  hint="sorted(scores, reverse=True) gives you a new list sorted highest-first, without mutating the original — then slice the first two with [:2]."
  correctAnswer={{
    code: `def top_two_scores(scores):
    return sorted(scores, reverse=True)[:2]`,
    explanation:
      "sorted(..., reverse=True) returns a new descending-order list in one step, and [:2] slices the first two items from it — combining sorting and slicing from this section.",
  }}
/>

</CheckpointZone>
