---
title: "Recap & Practice"
lesson: "08-containerize-with-docker"
order: 8
---
import QuizGroup from "../../../../components/lesson/QuizGroup";
import CheckpointZone from "../../../../components/lesson/CheckpointZone.astro";

<CheckpointZone>

<QuizGroup
  client:load
  questions={[
    {
      question: "Beyond making code run consistently across machines, what's the second motivation for containers this lesson introduces?",
      options: [
        "Containers make code run faster",
        "Restricting what running code can actually access — filesystem, network, credentials — as a genuine security boundary",
        "Containers eliminate the need for testing",
        "Containers automatically fix bugs in the code they run",
      ],
      correctIndex: 1,
      explanation:
        "Consistency is only half the story — a container also draws a real boundary around what the process inside it can touch, which matters just as much as making it run the same everywhere.",
    },
    {
      question: "What's the relationship between a Docker image and a Docker container?",
      options: [
        "They're the same thing, just different names",
        "An image is the fixed definition, like a class; a container is one running instance of it, like an object created from that class",
        "A container is the definition, an image is the running instance",
        "One image can only ever produce one container",
      ],
      correctIndex: 1,
      explanation:
        "An image never changes once built — every container started from it is its own independent, running instance, and one image can produce as many containers as you like.",
    },
    {
      question: "Why does ADD backup.tar.gz /app/ behave differently from COPY backup.tar.gz /app/?",
      options: [
        "They behave identically",
        "ADD automatically extracts a local archive's contents into the destination, rather than copying the archive file itself",
        "COPY doesn't work with compressed files",
        "ADD requires the file to already exist inside the container",
      ],
      correctIndex: 1,
      explanation:
        "ADD's extra behavior — auto-extracting a local archive — is exactly why COPY is the safer default: it always does the one obvious thing, copy the file as-is.",
    },
    {
      question: 'Given CMD ["python3", "app.py"], what happens if you run docker run my-app echo hello?',
      options: [
        "python3 app.py runs, then echo hello runs after it",
        "CMD is entirely replaced — the container runs echo hello instead",
        "It raises an error, since CMD can't be overridden",
        "Both commands run concurrently",
      ],
      correctIndex: 1,
      explanation:
        "A command given to docker run replaces CMD outright rather than combining with it — that's the whole reason CMD is described as a default, not a fixed command.",
    },
    {
      question: "Why can't a running container read an ARG value from its own environment at runtime?",
      options: [
        "This is a bug",
        "ARG only exists during the build itself; it's gone by the time a container actually runs, unlike ENV",
        "ARG values are encrypted",
        "ARG and ENV are identical in every way",
      ],
      correctIndex: 1,
      explanation:
        "ARG is scoped to the build — useful for things like a version number baked into a layer — while ENV is what actually persists into the running container's environment.",
    },
    {
      question: "What does adding a non-root USER instruction to a Dockerfile actually accomplish?",
      options: [
        "Nothing — containers are equally safe either way",
        "It limits what the running process can do even within the container's own boundary, instead of running with full root permissions by default",
        "It only affects who can build the image",
        "It's required for every Dockerfile to build",
      ],
      correctIndex: 1,
      explanation:
        "Container isolation doesn't by itself mean the process inside has no privileges — without USER, it runs as root, which is exactly the default this instruction changes.",
    },
    {
      question: "What does docker exec do?",
      options: [
        "Builds a new image from a running container",
        "Runs a command inside an already-running container, useful for inspecting or debugging it live",
        "Stops a container immediately",
        "Lists every image on the machine",
      ],
      correctIndex: 1,
      explanation:
        "docker exec is for reaching into a container that's already running — checking a file, poking around, debugging — without stopping it first.",
    },
    {
      question: "Why does data written inside a container disappear once that container is removed, even though the image it came from still exists?",
      options: [
        "This shouldn't happen",
        "A container's filesystem is its own, separate from the image and other containers — removing the container removes everything written into it",
        "Images automatically absorb changes made inside their containers",
        "Only docker stop causes data loss, not docker rm",
      ],
      correctIndex: 1,
      explanation:
        "Nothing written inside a container ever flows back into the image it came from — that filesystem belongs to the container alone, and removing the container removes it entirely.",
    },
    {
      question: "Why does changing one unrelated line in app.py trigger a full pip install reinstall, if COPY . . comes before it in the Dockerfile?",
      options: [
        "It shouldn't happen",
        "COPY . . sees the changed file, invalidating its own cache, which cascades forward and invalidates the install layer too",
        "pip install always reinstalls everything regardless of caching",
        "Editing app.py automatically edits requirements.txt",
      ],
      correctIndex: 1,
      explanation:
        "Docker's layer cache invalidates a layer and everything after it the moment anything upstream changes — which is exactly why COPY . . needs to come after the install step, not before it.",
    },
    {
      question: "How does a .dockerignore entry for .env help prevent secrets from ending up in an image?",
      options: [
        "It doesn't — unrelated to secrets",
        ".env is excluded from the build context before any COPY instruction runs, so it can't end up in the image regardless of how the Dockerfile is written",
        "It encrypts .env automatically",
        "It only works if .env is also listed in the Dockerfile",
      ],
      correctIndex: 1,
      explanation:
        ".dockerignore acts before the Dockerfile even runs — a file excluded from the build context was never available to be copied in the first place, no matter what the Dockerfile says.",
    },
  ]}
/>

</CheckpointZone>
